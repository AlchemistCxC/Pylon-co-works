# Pylon V7 设计书（修订版）— 中控区 + 布局重构

> 给 coder。这是 V7 完整版。先修 P0 bug，再做 UI 重构。

---

## 一、Bug 修复（P0）

同前版，不变：cacheHit（capabilities tokenStats）、溢出隐藏、chat header 友好名、CLI 切换修、ECG 行波+噪声、侧栏真隐藏。

---

## 二、布局重分块

```
┌─ 左栏 ──┬─ 对话窗口 (ChatView) ────────┬─ 右栏 ─┐
│         │                              │        │
│ 会话列表 │   消息渲染区                   │ 多页面  │
│ 搜索    │                              │        │
│ Profile │                              │        │
│ Agent   │                              │        │
├─────────┼──────────────────────────────┤        │
│         │  ┌─ 中控区 ────────────────┐  │        │
│         │  │ [输入栏] [附件] [命令]   │  │        │
│         │  │ [心电图] [模型] [模式]   │  │        │
│         │  └────────────────────────┘  │        │
└─────────┴──────────────────────────────┴────────┘
```

中控区 = 输入栏 + 信息栏合并。独立可配。

---

## 三、中控区（Control Center）

### 3.1 概念

一个可配置的容器区域。包含多个控件（widget），可拖拽排序、显隐开关、位置调整。有背景范围（影响背景图渲染）和大小范围（影响 ChatView 渲染区高度）。

### 3.2 控件清单

| 控件 | 描述 |
|:--|:--|
| `input` | 输入栏：CLI 风格（`>` 前缀 + 双横线，无发送按钮，Enter 发送）或 Default（圆角框 + 发送按钮） |
| `context` | 上下文显示：ECG 波形 / 水平柱状图 / 纯数字 |
| `attach` | 附件按钮（文件引用式） |
| `commands` | 快捷命令板（/ 命令列表） |
| `model` | 模型切换下拉 |
| `mode` | 权限模式切换（auto/edit/bypass/default） |

### 3.3 CLI 输入栏风格

```
═══════════════════════════════════════
> 用户输入文字在这里.............
═══════════════════════════════════════
```

- 左侧 `>` 提示符，等宽字体，颜色跟随 accent
- 双横线（上下各一根），横线颜色可配
- **没有附件按钮、没有发送按钮**——Enter 直接发送
- 输入框透明、无边框、无圆角
- 背景范围设为 0 → 双横线与 ChatView 无缝融合（模拟终端）

### 3.4 背景范围 vs 大小范围

```
┌─── ChatView ────────────────────────┐
│                                     │
│  消息区域                            │
│                                     │
├─── 中控区大小范围 ───────────────────┤  ← 影响 ChatView 底部留白
│  ┌─ 背景范围 ──────────────────┐    │
│  │ [背景图/颜色填充]            │    │  ← 仅影响背景渲染
│  │  [心电图] [模型] [模式]      │    │
│  │  [输入栏................]    │    │
│  └────────────────────────────┘    │
└─────────────────────────────────────┘
```

- 大小范围：`--cc-height`（默认 120px），决定 ChatView 底边距
- 背景范围：`--cc-bg-height`（默认 = 大小范围），可 < 大小范围。0 = 无背景，控件浮在 ChatView 上方
- 背景范围设为 0 → CLI 模拟终端模式：双横线 + 输入框直接叠在消息区底部

### 3.5 控件布局

控件顺序和位置可配置。store 存 `ccLayout: string[]`——控件 ID 数组，中控区按数组顺序从左到右排列。

```
ccLayout: ['context', 'model', 'mode']  // 第一行
ccLayout2: ['attach', 'input', 'commands']  // 第二行
```

或者更简单：一行排列，flex-wrap。

Settings → 中控区 → 可视化编辑器：显示所有控件，拖拽排序，勾选显隐。

### 3.6 上下文显示变体

| 变体 | store 值 | 描述 |
|:--|:--|:--|
| 心电图 | `wave` | 当前 ECG SVG |
| 柱状图 | `bar` | 水平进度条，颜色随 % 变 |
| 纯数字 | `numeric` | 仅百分比 + token 计数 |

柱状图设计：

```
████████████░░░░░░░░░░  42%  12.5k/128k · 3% hit
```

颜色阈值同 ECG：绿→黄→红。

---

## 四、设置面板重组织

当前 Settings 有 8 个分类（全局/左栏/终端/工具颜色/输入栏/状态栏/右栏/Agent）。V7 重组为 5 个：

| 旧 | 新 | 内容 |
|:--|:--|:--|
| 全局(部分) | **全局** | 玻璃效果 + 字体 + 背景图拖拽上传 + 适应模式(stretch/cover/contain/tile) + **用户信息挪到这**（userName/userPrefix/userColor，从原先散落各处集中） |
| 输入栏 + 状态栏 | **中控区** | 控件管理 + 输入栏风格 + 上下文样式 + 背景大小/范围 + CC 高度 |
| 终端 + 工具颜色 | **终端** | 字体/字号/行高 + 文字颜色 + 代码颜色 + 工具指示器颜色/形状 + sparkles + 消息栏风格 |
| 左栏 | **左栏** | 不变：背景色/图、栏宽、文字颜色、会话名字号、分组字号 |
| 右栏 | **右栏** | 右栏宽度 + **多页面配置**（增删排序页面） |
| Agent | 去掉 | Agent 设置直接放到标题栏 ⚙ 旁边或全局里作为子项 |
| — | **新增: 会话设置** | 见第五节 |

### 4.1 全局 Settings 结构

```
全局
├─ 用户信息              ← 新增，从终端 section 挪来
│   ├─ 显示名: [Txt]
│   ├─ 前缀: [Txt] 默认 ❯
│   └─ 颜色: [Swatch]
├─ 玻璃效果
│   ├─ 背景图: [拖拽区/Txt] + 适应模式 [Sel: stretch|cover|contain|tile]
│   ├─ 透明度: [Slider 0-1]
│   └─ 模糊: [Slider 0-40px]
└─ 字体
    ├─ 字体: [Sel: system|mono]
    └─ 基础字号: [Num 12-24]
```

### 4.2 中控区 Settings 结构

```
中控区
├─ 布局
│   ├─ 中控区高度: [Num 80-400]
│   └─ 背景范围: [Num 0-400]（0=透明融入ChatView）
├─ 控件显隐
│   ├─ 输入栏: [✓]
│   ├─ 上下文显示: [✓]
│   ├─ 附件按钮: [✓]
│   ├─ 命令板: [✓]
│   ├─ 模型切换: [✓]
│   └─ 模式切换: [✓]
├─ 输入栏风格
│   ├─ 风格: [Sel: default|cli]
│   ├─ 横线宽度: [Num 1-6]
│   └─ 横线颜色: [Swatch]
├─ 上下文显示 ← 原状态栏 section
│   ├─ 仪表样式: [Sel: wave|bar|numeric]
│   ├─ 心电图宽度: [Num 120-400]
│   └─ 心电图样式（基线宽度/振幅/波速/端点颜色等）
└─ 背景
    ├─ 背景色: [Swatch]
    └─ 背景图: [拖拽区/Txt]
```

### 4.3 终端 Settings 结构（合并原工具颜色）

```
终端
├─ 外观
│   ├─ 背景色/图
│   ├─ 字体/字号/行高
│   ├─ 文字颜色
│   ├─ 内联代码颜色
│   └─ 代码块背景
├─ 消息栏
│   ├─ 风格: [Sel: terminal|bubble]
│   ├─ AI 文字颜色
│   └─ 行间距
├─ 工具 ±
│   ├─ 工具名颜色
│   ├─ 摘要颜色
│   ├─ 指示器形状: [Sel: ●|◆|■|▲|▶]
│   ├─ 指示器颜色: 完成/运行中/错误
│   └─ 火花字符: [Txt]（Spinner 动画用）
└─ 用户标签
    ├─ 标签背景
    └─ 标签文字
```

---

## 五、会话设置（Per-Session，从侧栏进入）

**入口**：左侧栏每个会话项右侧 `⚙` 图标（hover 显示），点击打开会话设置弹窗。

不是 Settings 面板的子页面——是独立弹窗，跟 ProfileEditor 同级。每个会话独立存储。

```
┌─ 会话设置: "TRPG 战役" ──────────────────────┐
│                                                │
│  名称: [TRPG 战役                            ] │
│  平台: [本地 ▼]  (local/qq-group/qq-dm)        │
│  工作目录: [G:\Project\prism                 ] │
│                                                │
│  会话 Prompt（覆盖 Profile persona）:           │
│  ┌────────────────────────────────────────┐   │
│  │ 你是一个 TRPG 游戏主持人...              │   │
│  └────────────────────────────────────────┘   │
│                                                │
│  Skills:                                       │
│  [/compact ✕] [/export ✕]  [+ 添加]            │
│                                                │
│  Hooks:                                        │
│  [on_session_start ✕]  [+ 添加]                 │
│                                                │
│  ──────────────────────────────────────────    │
│  [保存]  [取消]              [删除会话] 红色    │
└────────────────────────────────────────────────┘
```

**兼容性**：agent 不支持 sessionPrompt/skills/hooks 时保存到 localStorage，不传 ACP。前端通过 persona 注入（已有逻辑）fallback。

**流程**：侧栏 → ⚙ → 弹窗 → 修改 → 保存到 store.session → localStorage 持久化 → 下次 load 时读取。

### 删除会话

移到每个会话项最右侧，hover 显示红色 `✕`，点击 `window.confirm("删除会话？")`（已实现）。位置从 `session-del` 的 flex 改为 `margin-left: auto` 确保贴右。

---

## 六、右栏多页面

右栏改为可配置的多页面容器：

```typescript
rightPages: { id: string; title: string; type: 'files'|'prism'|'rag'|'logs'|'custom'; order: number }[]
```

用户可在 Settings → 右栏 中增删、排序页面。

---

## 七、心电图端点修正

**基线宽度**：`strokeWidth` 从 3 改为 5。

**定端点 → 双竖线**（电表刻度符号）：

```tsx
<line x1="0" y1={mid-10} x2="0" y2={mid+10} stroke="rgba(0,0,0,0.35)" strokeWidth="2"/>
<line x1="3" y1={mid-10} x2="3" y2={mid+10} stroke="rgba(0,0,0,0.35)" strokeWidth="2"/>
<line x1={W} y1={mid-10} x2={W} y2={mid+10} stroke="rgba(0,0,0,0.35)" strokeWidth="2"/>
<line x1={W-3} y1={mid-10} x2={W-3} y2={mid+10} stroke="rgba(0,0,0,0.35)" strokeWidth="2"/>
```

**动端点 → 单竖线**（指针）：

```tsx
<line x1={cut} y1={mid-10} x2={cut} y2={mid+10} stroke={color} strokeWidth="3"/>
```

---

## 八、给 coder

1. P0 bug 先修（cacheHit 一行 `tokenStats: true`）
2. 中控区可以先做 CLI 风格 + 背景范围，拖拽可视化后续
3. 会话设置独立面板
4. 分批 commit，不 build
