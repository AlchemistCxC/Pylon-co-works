# L.md · 并行施工协调板

> 规则（AGENTS.md §2.3-4）：并行多 agent 施工时，在此声明施工范围以应对冲突（文件互相改写、连带提交等），**写入后立刻提交本文件**使其他 agent 可见。只追写，不覆写，留言简洁。

> **只留在途。** 本文件的价值是「谁正在改哪些文件」；已完工的条目占用读取代价，并且**文件越长、两边各自追加就越容易冲突**（本文件历史上多次成为合并冲突点）。所以自己的 issue 合入后即可移除自己的条目。2026-09-16 及以前的条目（其 issue 均已有 `.agents/records/` 开发记录）已归档到仓外 `../Docs/Archive/L-archive-20260918.md`；2026-09-17 至 2026-09-25 的已完工条目（#110/#315/#316/#317 批次一/ADR 通审等）已归档到仓外 `../Docs/Archive/L-archive-20260925.md`；2026-09-26 撤下的已完工 [kumo] 条目（#324/#331/#334-336/#338/#339/#325-329，issue 均已 CLOSED 且改动已并入 main）已归档到 `../Docs/Archive/L-archive-20260926.md`；[Codex] #155 T3 已随 PR #347 并入 main 撤下。

- [kumo] #361+#362+#363 单 PR 批次（三个 issue 逐项完成后各派子 agent 审查）：
  - #361 → `src-tauri/src/main.rs`（crate 级 GUI 子系统属性）。**不碰**其他 `[[bin]]` 入口（`pylon-cli`/`pylon-detect`/`pylon-fake-agent` 仍为 console）。
  - #362 → 新 `src-tauri/src/logging/**`、`src-tauri/src/runtime_log/mod.rs`（Layer 自反馈隔离）、`src-tauri/src/session/mod.rs`（仅 :406-434 lag warn 节流区段）、`src-tauri/src/paths.rs`（日志目录）、`src-tauri/Cargo.toml`（+`tracing-appender`）、`src-tauri/vendor/acp/ORIGIN.md`（出处登记）。
  - #363 → `src-tauri/pylon-acp/src/{process,engine,terminal_runtime}.rs`（spawn 收口）、新 `src-tauri/pylon-core/src/node_path.rs`、`src-tauri/src/session/{expiry.rs,session_expiry_platform_tests.rs}`（空闲回收）。
  - ⚠️ **`src-tauri/src/lib.rs` 与 #371 声明重叠**：我只改 `init_tracing()`（#362 落盘 sink）与 `run()` 启动首段的 node PATH 修复；#371 的 mod/AppState/scheme/命令/setup 注册不在此批，两边改动点不相邻，可直接并存。
  - 规格 `.agents/spec/361-release-gui-subsystem.md`、`362-crash-forensics-logging.md`、`363-acp-subprocess-lifecycle.md`。
  - ⚠️ **在途冲突（2026-09-26 23:55 观测）**：工作树里同时有 #371 的未提交改动（`src-tauri/src/docs_sheet/**` untracked、`tauri.conf.json`、`.gitignore`、`browser/mod.rs`、`lib.rs` 的 `mod docs_sheet` 注册）+ 前端两件（`src/app/errorCodeExplanations.ts`、`src/domains/workbench/generationLedgerSummary.ts`）。因此**本批暂缓提交 `src-tauri/src/lib.rs`**——同一文件两边都有 hunk，连带提交会让 #371 的 untracked 模块变成悬空引用。其余本批文件无重叠，可安全按 pathspec 提交；对方收工后补上 lib.rs。
  - **不碰**：前端 `src/**`、`docs/说明书/**`、`src-tauri/src/lifecycle/mod.rs`（只调用既有 `stop_agent_runtime`，不改其实现）、#353/#330/#372/#371 域。
- [kumo] #351 前端根目录归类（#245 前端对应件）：`src/` 根 22 文件按域下沉（identity→`domains/identity/`、IPC 件→`infrastructure/{persistence,skin}/`、workspace/runtime/cc→各自 domains、error 三件→`app/`、其余就近）+ **全仓 import 路径重写**（触及 src 广泛文件的 import 行，语义零变更）+ 断 `identityStore→workspace/runtime`（新增 `src/app/bootstrap/identityCrossDomainSync.ts` 端口）+ `scripts/check-runtime-boundaries.mts` allowlist ×3 路径随迁 + `scripts/audit-maintenance.test.mts` 断言随迁 + 说明书文件链接同步。规格 `.agents/spec/351-frontend-root-file-relocation.md`。**不碰** `store.ts`/主题预设集群与 `presets//zones/`（#266 域）、`src-tauri/**`（#348/#349 在途）、入口四件。
- [kumo] #348+#349 ACP 连接缺陷批次（单 PR，两个施工 agent 按文件域互斥并行）：
  - #348 → `src-tauri/pylon-acp/**`（**不含 `src/adapter/`**）、`src-tauri/pylon-acp/Cargo.toml`、`src-tauri/Cargo.toml`、`src-tauri/vendor/acp/ORIGIN.md`：写侧崩溃终因词表一致性 / 版本声明对齐 / spawn 补 CREATE_NO_WINDOW / reducer 补 available_commands_update 臂 / `is_method_not_found` 兜底收窄 / protocol_version 接受集合 fail-closed。
  - #349 → `src-tauri/src/session/create.rs`、`src-tauri/src/session/fork.rs`（注释）、`src-tauri/src/dispatcher/interaction_route.rs`、`src-tauri/src/private_interaction.rs`、`src-tauri/pylon-acp/src/adapter/**`（自 #348 域切出）：revive 路径 session/load 回放治理 / elicitation request-scoped 入桥（广告⇔执行不一致）。
  - 规格 `.agents/spec/348-acp-engine-core-defects.md`、`.agents/spec/349-host-session-lifecycle-defects.md`。
  - **两边都不碰**：`src-tauri/src/dispatcher/mod.rs`、`pylon-session/**`、前端、`docs/说明书/**` 之外的文档。
  - 明确留给后续 issue（本批不做）：fs/terminal 错误码细化（改 agent 侧 wire 码）、`-32000 AuthRequired` 语义消费、Windows `.cmd/.bat` + UNC cwd 绕行、cancel settle 窗口（待复核）。
  - 返工轮（独立审查反馈）：触及 `src-tauri/src/acp/tests.rs`（A6 测试裁决改写）与大批原域内文件；**前端死词条清理（A1 摘除 `WriterTimeout` 后的 `errorCodeExplanations.ts` 等）不在本批**——那些路径属在途 #351（`e0538758` 已下沉 error 三件到 `src/app/`），改由 issue 跟进。
  - 审查结论（独立 agent）：A1 误报审查通过、A6 裁决成立、#349 对规格的机制更正成立、B4 跳过理由成立；必修项为「B2 条件宽化」与「B2 端到端缺前端应答门（已回退准入，转 issue #356）」。
- [Penrose] #358 复活会话缺协商事实（配置卡消不掉）：`src/sheets/agent-workbench/sessionResponseProjection.ts`（新增可选 `syntheticReason` 尾参，默认值不变）、`agentWorkbenchSession.ts`（`applySessionResponse` 透传标注 + pending 携带）、`agentWorkbenchLifecycle.ts`（新增宿主缝 `onSessionLoadResponse`）、`AgentRendererSuiteWorkbench.tsx`（接线）；用例新增在 `src/sheets/agent-workbench/__tests__/` 与 `src/renderers/solid-workbench/__tests__/`。规格 `.agents/spec/358-revive-negotiation-surface.md`。**不碰** `WorkbenchDocumentSurface.solid.tsx` 的守卫语义、`src-tauri/**`、`pylon-session/**`、#351 域（`src/` 根件下沉）。
- [Penrose] #370 markdown 列表双符号：只改 `src/plugins/product/packages/builtin.pylon-renderers/styles/components/chat/ChatView.css`（关掉 markdown 列表的原生 marker / 关掉 ol 的 `::before`）+ `src/renderers/solid-workbench/chat/__tests__/ChatView.css.test.ts`（CSS 文本断言）。规格 `.agents/spec/370-list-marker-dedup.md`。**不碰** 其他样式文件与渲染器 tsx。
- [kumo] #372 发行包内容清偿：`resources/release/**`（README.txt 重写 Hermes 段、agents.example.yaml→agents.template.yaml、删 tools/repair-hermes-acp.{bat,ps1}）+ `scripts/pack_release.py` + `scripts/tests/test_pack_release.py` + `docs/说明书/{Pylon-发行包清单,Pylon-项目架构参考}.md` + `src-tauri/src/agent_config/embedded_agents.yaml`（仅注释）。规格 `.agents/spec/372-release-package-content-cleanup.md`。**不碰** 仓库根 `agents.example.yaml`（tests.rs 夹具）、`src-tauri/src/**` 代码、`src/` 前端。
- [kumo] #373 solidRendererSurface 冷挂载预算：只改 `src/plugins/core/renderer/__tests__/solidRendererSurface.test.ts`（用例先预热 `loadSolidMessageRendererComponent()` 把冷启动移出 2s 预算）+ `.agents/{spec,records}/` 记录。**不碰** `solidRenderer.ts`、loader 模块、vitest 配置与其他测试文件。规格 `.agents/spec/373-solid-renderer-cold-mount-budget.md`。
- [kumo] #371 离线文档站进发行包：`docs/.vitepress/config.ts`（离线变体：base 回根 + 裁 Web 字体）+ 新 `scripts/stage-docs-site.mjs` + `package.json`（docs:build:offline、release:portable 链插一步）+ `src-tauri/tauri.conf.json`（bundle.resources + csp/devCsp 加 pylon-docs:）+ 新 `src-tauri/src/docs_sheet/**` + `src-tauri/src/lib.rs`（mod/AppState/scheme/命令/setup 注册）+ `src-tauri/src/browser/mod.rs`（仅 host_additional_browser_args 提 pub(crate)）+ 前端新增 `src/sheets/docs/`、`src/infrastructure/tauri/docsClient.ts` + `src/plugins/core/sheet/builtinWorkspacePlugins.ts`（注册一行）+ `src/workspace-sheets/launchIcons.{tsx,solid.tsx}`（book-open 键）+ `.gitignore`（+/src-tauri/resources/docs-site/）。**避让 #372**：`scripts/pack_release.py`、`scripts/tests/test_pack_release.py`、`docs/说明书/Pylon-发行包清单.md` 待其收工后再落 docs-site 检查与清单行。**不碰** #358/#370/#373 域与 `resources/release/**`、`embedded_agents.yaml`。规格 `.agents/spec/371-offline-docs-site.md`。⚠️ 12a8acd7 是失败的 board 提交：误带入 #372 的三个 staged 删除（agents.example.yaml、repair-hermes-acp.{bat,ps1}，内容即 #372 计划内改动，**删除已进历史，#372 收工提交无需再删**），而 #371 自己的 L.md 行未入内，由本条补上。
  - **进展（2026-09-27 00:2x）**：除 `src-tauri/src/lib.rs` 外全部已入库（构建链 ac297a37、Rust a2cbe02e、前端 f959b48c）。**`lib.rs` 的 #371 hunk 请 #361-363 批次随其 lib.rs 一并连带提交**（`docs_sheet/` 已在 a2cbe02e 入库，连带无悬空），#371 不再单独提交 lib.rs。`pack_release.py`/`test_pack_release.py` 避让解除（#372 已收工），#371 现改这两件；`docs/说明书/Pylon-发行包清单.md` 仍在 #361 在途（其工作树 hunk），继续避让。
  - **收尾（2026-09-27 00:4x）**：pack_release/清单均已入库（486bca6b、7a713bc2），开发记录 a79afd42，issue 证据已回写，PR＝#374（共享分支随带，已在 PR 评论区写明 #371 处置与「合并前确认 lib.rs hunk 已入库」前置）。**本条目仅剩一件在途：lib.rs 五处接线 hunk 待 #361-363 连带提交**，其余视为完工，提交后即可撤条。
- [kumo] #357 前端死词条清理 + 词表对齐门禁：`src/app/errorCodeExplanations.{ts,+__tests__}`（删 `writer_timeout`、`writer_failed` 文案对齐 #348 语义、崩因节提为导出常量）、`src/domains/workbench/generationLedgerSummary.{ts,+__tests__}`（删 `writerTimeout`、失败侧集合导出）、新 `scripts/acp-vocabulary.test.mts`（Rust 词表↔前端表静态比对，vitest 纳管）。**不碰** `package.json`、`src-tauri/**`（只读解析）、#361-363/#371 域。规格 `.agents/spec/357-acp-vocabulary-cleanup.md`。
- [kumo] #354 ACP 错误码语义（fs/terminal 负路径 + AuthRequired 消费）：`src-tauri/pylon-acp/src/{fs_policy,file_system_runtime,error,cause}.rs`（fs 负路径三分类 `FsFailure`；`RpcFailureKind::AuthRequired`；initialize 对 `-32000` 给稳定码 `agent_auth_required`）+ `src-tauri/src/dispatcher/mod.rs`（fs/terminal 错误→wire 码映射，data 走官方 SDK `Responder::respond_with_error` 直发，**不改 engine.rs**——避让 #363）+ `src-tauri/src/acp/tests.rs`（`-32000` 样本按新契约修正）+ 前端 `src/app/errorCodeExplanations.ts` 及其测试**仅追加** `agent_auth_required` 一条（区域与 #357 的删词条/提常量不相邻）+ 说明书两处行。**不碰** `pylon-acp/{process,engine,terminal_runtime}.rs`、`pylon-core/**`、`src-tauri/src/{main.rs,logging/**,session/**}`（#361-363 域）。规格 `.agents/spec/354-acp-error-code-semantics.md`。
- [kumo] **内存峰值调查（已登记，未开工）**：#376 canonical 读路径载荷双份（`typed_payload` 不截且前端不读 / `raw_payload` 截 64KiB）+ 冷装载一次性全量下发 → 合成 61.5MB 工具载荷实测 renderer 1750MB / `pylon.exe` 1438MB / 总计 2638MB；#375 投影层载荷三重持有（`timeline[].data` + `activities[]` 的 `structuredClone` + `fold.log` 信封 raw）+ 工具拍不折叠 → 驻留 = Σ载荷 ×2.13。记录 `.agents/records/375-376-memory-payload-amplification.md`，规格 `.agents/spec/375-376-memory-payload-amplification.md`。**本轮只登记 + 落规格，未开工（用户指示）**，故不占文件域；开工时的拟触域：`src-tauri/pylon-session/src/event_repo/**`、`src-tauri/src/session/mod.rs`、`src/infrastructure/events/canonicalEventRepository.ts`（#376）；`src/domains/workbench/workbenchProjector.ts`、`src/sheets/agent-workbench/agentWorkbenchSession.ts`、`src-tauri/pylon-session/src/turn_rollup.rs`、`src/renderers/solid-workbench/**`（#375）。**避让** #348/#349/#361-363/#371/#372 既有域；**请勿重复调查**：静态审核与两轮实机验收（真机 + 隔离副本合成重放）已完成，读数见记录。
- [kumo] #352 用户 cancel 一等判死输入：**独立 worktree 分支 `kumo/352-cancel-death`（基于 github/main）**——共享树 `engine.rs` 属 #363 在途脏文件（§2.1 禁触碰），按 §2.5 并行冲突开分支条款隔离施工，共享树内只动 L.md 与 `.agents/spec/`。改动域（均在 worktree 内）：`pylon-acp/src/engine.rs`（`wait_prompt_with_recovery` 增 cancel 判死输入、`PromptTimeoutKind` 增 UserCancel）+ `src-tauri/src/session/{model,prompt,control}.rs`（载体 + 置位/清除 + 闭包接线）+ 对应测试。**不碰** 共享树任何 `src-tauri/**`。规格 `.agents/spec/352-user-cancel-first-class-death.md`。
