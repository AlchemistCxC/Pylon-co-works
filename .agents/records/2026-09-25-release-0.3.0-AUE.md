# Dev Record — release 0.3.0-AUE（分支收敛 + 便携包发行）

> 入库保留。本次为发行性任务，无独立 issue 号，沿用日期命名。
> 产出路径：`.agents/records/2026-09-25-release-0.3.0-AUE.md`

## 元信息

- issue：无（发行任务，承接用户指令：清理 kumo 分支 / main 同步到唯一分支 / 构建并上传 0.3.0-AUE）
- 分支：`kumo/prometheus`（清理后唯一的 kumo 分支）
- 提交范围：`a485465e..196da8cf`（版本落位）；tag `v0.3.0-AUE` 指向 `196da8cf`
- 日期：2026-09-25
- 环境：bun / rustc stable-msvc / Python 3 / Windows
- 构建纪律：`CARGO_TARGET_DIR=D:\pylon-acceptance-target`（#228；G 盘仅余 1.9G，不写 G 盘 target）

## 目标与范围

**目标**：① 清理 `kumo/*` 分支只留 `kumo/prometheus`；② `github/main` 同步到该分支；③ 版本号升 `0.3.0-AUE`；④ 跑完 `release:portable` 全链路并发行（tag + GitHub Release + 三件套附件）。

**不做什么**：不改任何功能代码；不动 `docs/说明书/`；不重写历史。

## 改动清单

| 文件 | 大致范围 | 性质 |
| --- | --- | --- |
| `package.json` | `version` 字段 | 修改 |
| `src-tauri/tauri.conf.json` | `version` 字段 | 修改 |
| `src-tauri/Cargo.toml` | `[package] version` | 修改 |
| `src-tauri/pylon-{acp,canonical-types,compute,markdown,session}/Cargo.toml` | `[package] version`（5 crate） | 修改 |
| `src-tauri/Cargo.lock` | 6 处工作区成员 version | 修改 |
| （仓外）`D:\pylon-acceptance-target` | release 构建缓存 | 新增 |
| `release/pylon-0.3.0-AUE-win64.{zip,zip.sha256,manifest.json}` | 发行产物（release/ 不入库，随 GitHub Release 上传） | 新增 |

## 分支收敛

- `kumo/prometheus` 快进合并 `github/main`（`c3c88843..a485465e`，纯快进无冲突），已推送。
- 删除已完全入 main 的分支（本地 + 远端）：`kumo/315-normalizer`（450afe67 入 main）、`kumo/filesheet-stage0`（a0dab091 入 main，PR #319 已合并）、`kumo/solidify`（a7056ec4 入 main；其 worktree `D:/pylon-solidify-wt` 干净且无未提交改动，先 `worktree remove` 再删分支）。
- 保留：`kumo/prometheus`（唯一）。非 kumo 前缀分支（`Ru5t/*`、`codex/*`、`fix/*`、`main`）未动。
- 收敛前对 `kumo/filesheet-stage0` 工作树未提交改动（他人在途的 `.agents/decisions/*` 等 26 文件，main 已含更新终版）做了 `git stash push -u` 备份（`stash@{0}`，可追溯），避免切换分支丢工作。

## 构建与发行

- 版本号 `0.3.0-AUE` 落位 8 处 + `Cargo.lock` 刷新（cargo check 触发）。
- `CARGO_TARGET_DIR=D:/pylon-acceptance-target bun run release:portable` 全链路：前端 `bun run build`（wasm + tsc + vite）→ 插件 SDK → `tauri build --no-bundle` → `pylon-detect` → `webview2-mcp` → `pack_release.py` 打包。**EXIT=0**。
- 打包脚本 verify：ZIP 244 条目、manifest 与 ZIP 内容一致（242 项）、sha256 落盘。
- token 锚点的 tag：`v0.3.0-AUE`（annotated，指向 196da8cf，已推送）。
- GitHub Release：`https://github.com/AlchemistCxC/Pylon-co-works/releases/tag/v0.3.0-AUE`，附件 zip 37,572,963 B / sha256 / manifest.json。

## 验收标准与结果

| 验收项 | 结果 |
| --- | --- |
| 分支只剩 `kumo/prometheus` | ✅ `git branch` 与 `git branch -r` 均只余该分支 |
| prometheus == main（收敛时点） | ✅ HEAD = a485465e = github/main |
| 构建全链路 | ✅ EXIT=0，无 error |
| 产物完整性 | ✅ verify OK（manifest ⇔ ZIP 逐项）；sha256 已生成 |
| 打包 exe 实机冒烟 | ✅ 解压后启动：CDP 页面 "Pylon" 可达；exe VersionInfo FileVersion/ProductVersion = 0.3.0-AUE；manifest version = 0.3.0-AUE；冒烟实例与解压目录已清理 |
| Release 上传 | ✅ 三件套附件就位 |

## 证据

- 构建日志：`/d/pylon-build-0.3.0-AUE.log`（仓外，`EXIT=0` 与 `verify OK` 在尾部）
- 校验和：`release/pylon-0.3.0-AUE-win64.zip.sha256` = `5176e72de43d18b128f9565200299b8f0a0237952ff87d7f3f410ec97fe5903a`
- 冒烟：PowerShell VersionInfo 输出 0.3.0-AUE；CDP `http://127.0.0.1:9223/json/list` 页面标题 "Pylon"

## 与 spec 的偏差

无（发行任务无 spec；范围按用户指令执行）。

## 未解问题

- 打包 exe 冒烟为「启动 + 版本 + 页面加载」轻量级；未做真机 agent 全链路（#316 交付时已用 debug 实例完成过等价实机验收，release 二进制行为由同一源码导出）。
- 合并不涉及功能改动，故未重跑全量测试；版本落位提交基于 a485465e（其 CI 在 main 上为绿）。
