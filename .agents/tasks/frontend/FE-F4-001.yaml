version: 1
id: FE-F4-001
title: 消费 frozen Agent status v2 契约
lane: frontend
priority: P0
stage: agent-ui
subsystem: agent-status
size: M
relay_group: agent-ui
blocks_other_lane: false
domains: [settings-ui, workspace-sheet]
dependencies: {tasks: [BE-B0-001], checkpoints: []}
goal:
  user_result: Agent 列表和 Sheet 状态点按 registry ID 正确更新，显示名变化不导致状态丢失。
  technical_result: 更新 Agent types/normalizer/store key，兼容旧 payload 但优先 agentId。
source_facts:
  - 当前 App.tsx 使用 status.agent 作为 key。
  - 当前 agentTypes 缺 connecting/inactive 和 agentId/agentName。
scope:
  inspect_first:
    - .agents/contracts/agent-status.md
    - src/components/settings/agentTypes.ts
    - src/App.tsx 中 peri:agent-status listener
    - src/store.ts 中 agentStatuses actions
    - src/workspace-sheets/SheetTabStrip.tsx
    - 对应 Agent tests
  likely_modify:
    - 上述前端文件和 targeted tests
    - docs/前端开发与交接手册.md F4.1
  do_not_modify: [src-tauri/**, switch_agent 后端, Workspace menu]
guidance:
  invariants:
    - store key 是 agentId，不是 agentName。
    - 兼容 payload 不制造 connected 假状态。
    - inactive 是 UI 派生态时与后端连接态区分。
  non_goals: [Agent 配置写 UI, Sheet focus switch transaction]
verification:
  during_development:
    - 相关 Agent normalization/listener tests
    - npm run build
  checkpoint_only: [真实 Tauri list/status/event]
completion:
  - contract consumer 与 frozen 字段一致
  - 旧 payload 有明确兼容 fallback
opens_checkpoint: null
