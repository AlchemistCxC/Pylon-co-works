version: 1
id: BE-B10-002
title: 长回复切断 truncate（平台文本上限分段）
lane: backend
priority: P1
status: completed
stage: gateway-deliver
subsystem: gateway
size: S
relay_group: gateway
blocks_other_lane: false
domains: [gateway-deliver, text]
dependencies: {tasks: [], checkpoints: []}
goal:
  user_result: agent 超长输出按平台上限分段，代码块不被切断。
  technical_result: 新建 src-tauri/src/gateway/truncate.rs，实现 truncate_message 纯函数 + 详尽单测。
source_facts:
  - QQ 平台单条文本上限 4000 字符。
  - 参考实现 Hermes：F:\Hermes\hermes-agent\gateway\platforms\base.py 5527 起 truncate_message。
  - 设计定稿见 docs/后端开发与交接手册.md「长回复切断方案」。
scope:
  inspect_first:
    - docs/后端开发与交接手册.md 的 B10「长回复切断方案」
    - F:\Hermes\hermes-agent\gateway\platforms\base.py 的 truncate_message
  likely_modify:
    - src-tauri/src/gateway/truncate.rs（新建）
  do_not_modify:
    - src/**
    - src-tauri/src/lib.rs
    - src-tauri/src/acp.rs
guidance:
  invariants:
    - 纯函数签名 truncate_message(content, max_length) 返回 Vec of String
    - 超限才切；分段规则：代码块 fence 保留（段尾闭三个反引号 + 下段原语言标签重开）、分段指示器 (1/N) 预留 10 字符、自然切点（换行优先，其次空格，最后硬切）
    - 不依赖 tauri/AppState
  non_goals:
    - 发送队列/失败重试
    - 平台适配器接线
verification:
  during_development:
    - cd /g/Project/prism-desktop/src-tauri && cargo check --offline
    - cargo test --offline --lib gateway::truncate
  checkpoint_only: []
completion:
  - truncate.rs 实现 + 单测覆盖：短消息原样、超长分段、代码块内切段 fence 保留、指示器、中文/emoji 长度
  - cargo check 零 warning
opens_checkpoint: null
