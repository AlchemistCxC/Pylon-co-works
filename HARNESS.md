# Harness — prism-desktop Release 问题诊断工作规范

## 任务定位
你在处理 G:\Project\prism-desktop 项目的 release 问题诊断。
工作模式：只诊断 + 产出规格化文档。不直接修改代码、不提交，除非宫木云明确要求。

## 项目侦察（仅首次会话做一次，轻量）
- 用文件搜索扫一遍目录结构，掌握模块划分（src 下有哪些模块、入口在哪）
- 只读关键文件：入口、路由、状态管理、核心逻辑模块；每文件只读相关片段
- 不阅读任何 *.md 文档，不全量阅读任何文件
- 目标：建立“模块 → 文件路径”索引即可，控制上下文占用

## 工作循环
宫木云逐个汇报 release 问题（一次一个，自带触发条件/复现步骤）
→ 你定位根因、给出方案
→ 追加写入规格化文档
→ 等下一个问题
在宫木云提出下一个问题前，不主动展开新问题。

## 规格化文档
路径：G:\Project\prism-desktop\docs\release-issues.md
规则：追加式；问题编号全局递增（#1、#2、#3…），跨会话不重置；已有条目不回改，如需修正只追加说明。

### 单问题条目格式
问题编号：#N
严重度：P0 崩溃/主流程不可用 | P1 功能错误 | P2 体验/次要问题
状态：待处理 → 已交付（方案已写入）→ 待验证 → 已解决（宫木云确认后更新）
问题现象：宫木云汇报的原话 + 触发条件（复现步骤）
问题根因：唯一根因，必须查证属实。证据分三级：
  - L1 直接证据：日志报错、复现实验、调试输出
  - L2 源码证据：代码路径推演，引用具体文件 + 行号
  - L3 推断：无法查证时必须标注【待验证】，并列出已排除的假设
相关源代码：文件路径 + 行号 + 关键代码片段（精准引用，不贴整段）
解决方案：代码级详细，可并列多个。每个方案包含：
  - 改动位置（文件 + 位置）
  - 具体改法（代码级，能直接落地）
  - 影响面：是否改变现有业务行为
  - 验证方式：如何测试该方案有效
  - 风险与取舍；多方案时标出推荐项
  - 需宫木云拍板时标注【需拍板】
重构方案（可选）：问题集中在一处或属架构级缺陷时写——说明架构层面需要怎么优化及理由

### 查不到根因时的兜底
诚实标注【未能定位根因】+ 已排除的假设 + 下一步调查方向。禁止编造。

## 每轮交付
1. 正文回复：一句话根因结论 + 严重度 + 方案概述
2. 文档已更新（说明新增/修改了哪个编号）
