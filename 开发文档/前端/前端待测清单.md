# Prism Desktop 前端待测清单

说明：每个前端 Task 完成时必须更新本文件。条目通过后保留证据，不直接删除。

## BUG-P0-02：会话切换消息 localStorage 隔离

- 状态：已实施，待真实运行时测试
- 修改范围：`src/components/chat/ChatView.tsx`、`src/components/chat/messagePersistence.ts`、`scripts/test-message-persistence.mts`
- 自动验证：
  - `node --experimental-strip-types scripts/test-message-persistence.mts`：exit 0，覆盖 owner 一致、A→B 串写守卫、owner 清理三条路径。
  - `npm run build`：exit 0，3296 modules transformed。
  - `git diff --check`：exit 0。
- 运行时步骤：
  1. 创建 A、B 两个有不同历史的会话。
  2. 连续执行 A→B→A→B。
  3. 检查 `pylon-msgs-<A.id>` 与 `pylon-msgs-<B.id>`。
  4. 重启应用后重新打开 A、B。
- 验收：历史不串写、不丢失，重启后仍隔离。
- 测试结果：自动化回归通过；真实 Tauri A→B→A→B 与重启恢复待填写。

## BUG-P0-03：后台会话事件路由

- 状态：已实施，待真实运行时测试
- 修改范围：`ChatView.tsx`、`InputBar.tsx`、`Settings.tsx`、`store.ts`、`sessionEventState.ts`、`scripts/test-session-event-state.mts`
- 自动验证：
  - `node --experimental-strip-types scripts/test-session-event-state.mts`：exit 0，覆盖后台 A 更新不污染 B、generating 按 source 去重、A done/error 只清理 A。
  - `node --experimental-strip-types scripts/test-message-persistence.mts`：exit 0。
  - `node --experimental-strip-types scripts/test-replay-state.mts`：exit 0。
  - `npm run build`：exit 0，3297 modules transformed。
  - `git diff --check`：exit 0。
- 运行时步骤：
  1. A 开始生成后立即切换到 B。
  2. 在 B 停留直到 A 完成或报错。
  3. 切回 A，检查文本、reasoning、tool、done/error。
  4. 同时观察 A/B 的发送/停止按钮与 generating 状态。
- 验收：A 后台事件完整；A done/error 正确收敛；B 不受污染。
- 测试结果：自动化路由状态回归通过；真实 Tauri 多会话流式事件待填写。

## BUG-P0-04：session/load replay 恢复状态机

- 状态：待测试
- 类型：跨端
- 修改范围：`ChatView.tsx`、`replayState.ts`
- 自动验证：
  - `node --experimental-strip-types scripts/test-replay-state.mts`：通过。
  - `npm run build`：exit 0，3295 modules。
  - `git diff --check`：exit 0。
- 运行时步骤：
  1. 打开包含多轮历史的 persisted session。
  2. 检查历史是否只出现一次。
  3. 检查 replay 期间 spinner 与 `liveGeneratingSources`。
  4. 检查 model/configOptions。
  5. A→B→A 切换并重启恢复。
  6. 模拟 load 失败，检查 localStorage fallback。
- 验收：Peri replay 成功时为唯一真值；不重复、不启动 generating；load 失败才使用缓存。
- 后端依赖：透传 `periReplay` 并返回完整 load response。
- 测试结果：待填写。
