# Prism Desktop 后端交接文档

更新时间：由每次后端 Task 完成者更新。

## 当前已实施、待测试

### BUG-P0-01

- 根因：timeout 原实现只删除本地 pending 并立即释放同 source prompt lock；虽然发送了 cancel，但未等待 Peri 最终 response，重试可能与尚未收敛的旧任务重叠。`call_async/prepare_prompt` 的序列化失败路径还会遗留 pending。
- 实现摘要：抽出 `wait_prompt_with_cancel`；timeout 后发 `session/cancel` 并继续等待最终 response 30 秒；等待期间保留 per-source lock。未收到最终 response 时移除匹配的本地 session 映射并 best-effort `session/close`，使后续重试创建新 session。序列化失败会删除 pending。
- 修改文件：`src-tauri/src/acp.rs`、`src-tauri/src/lib.rs`、`开发文档/后端/后端待测清单.md`、`开发文档/后端/后端交接文档.md`。
- 对照 Peri 真值：
  - `F:\A-I\Agent\Peri\peri-tui\src\acp_stdio\session\prompt.rs`：prompt 后台执行，cancel token 注册到 session。
  - `F:\A-I\Agent\Peri\peri-tui\src\acp_stdio\session\control.rs`：`session/cancel` 按 sessionId 触发 token。
  - `F:\A-I\Agent\Peri\peri-tui\src\acp_stdio\session\prompt_exec.rs`：取消后仍发送最终 `PromptResponse`，随后清理 cancel token。
  - `F:\A-I\Agent\Peri\peri-tui\src\acp_server\mod.rs`：参考 per-session prompt serialization。
- 并发/生命周期不变量：同 source prompt lock 覆盖正常 response 或 cancel 收敛边界；不同 source 仍可并行；未收敛的 periId 不再被本地映射复用；timeout/cancel 路径删除 pending。
- 新增测试：`timeout_sends_cancel_and_waits_for_final_response`、`response_before_timeout_does_not_send_cancel`。
- 验证证据：
  - `unset RUSTFLAGS; cargo check` exit 0。
  - `git diff --check` exit 0。
  - 两个 `cargo test --lib ...` 均完成编译，但 test binary 启动失败，进程状态 `0xc0000139 STATUS_ENTRYPOINT_NOT_FOUND`，shell exit 127，未进入 test harness。
- 未验证：真实 fake ACP cancel 参数抓取；同 source 等待收敛、不同 source 并发；迟发 notification 的 turn 隔离。
- 剩余风险：ACP notification 没有 request/turn identity，30 秒收敛期的旧 chunk 仍按原 source 路由；prompt lock map 未清理；未实现通用 Agent generation。
- commit：`32a2d70`，文件：`src-tauri/src/acp.rs`、`src-tauri/src/lib.rs`、后端待测清单、后端交接文档。

### BUG-P1-01

- 实现摘要：trim 后空 Session Prompt 回退 Profile persona。
- 未验证：None/空/空格/非空四组运行时输入。

### BUG-P1-02

- 实现摘要：无 RPC error 的 response 后才提交 `has_first_prompt`。
- 剩余风险：业务假成功仍可能提交，依赖 BUG-P1-09。

### BUG-P1-03

- 实现摘要：prompt 等待不再持有全局 creation lock；同 source 使用独立锁。
- 未验证：A 生成时 B 发送、新建 C、A 第二条 prompt。
- 剩余风险：尚无 ACP client generation。

### BUG-P1-05

- 状态：待测试
- 根因：`AcpClient::connect` 在 spawn 后直接使用裸 `Child`。stdin/stdout/stderr 获取失败或 initialize 返回 RPC error/timeout 时，`?` 提前返回，`Child` 的默认 Drop 不会终止或 wait 子进程，导致 Peri 孤儿进程。
- 实现摘要：新增 `ManagedChild` RAII guard。spawn 后立即接管 child；所有 connect 失败出口自动执行 `try_wait`，仍运行则 `kill + wait`；连接成功后 guard 作为 `AcpClient` 字段继续管理，显式 `kill()` 与 Drop 复用同一清理逻辑。
- 修改文件：`src-tauri/src/acp.rs`、后端待测清单、后端交接文档。
- 对照 Peri 真值：
  - `F:\A-I\Agent\Peri\peri-tui\src\acp_stdio\mod.rs`：initialize 是连接建立后的首个 ACP request。
  - `F:\A-I\Agent\Peri\peri-tui\src\acp_stdio\transport.rs`：initialize handler 通过 responder 返回响应。
  - `F:\A-I\Agent\Peri\peri-tui\src\acp_stdio\init.rs`：initialize 前 Peri 已完成 provider、MCP、插件、ThreadStore 等进程级初始化，失败/超时期间子进程可能保持运行。
- 生命周期不变量：spawn 后任意失败出口均拥有 child cleanup guard；kill 后必须 wait；已退出 child 不重复 kill；成功 client 的 Drop 也不遗留进程。
- 新增测试：未新增可运行进程测试；当前 Windows GNU/Tauri test binary 无法进入 harness。
- 自动验证：
  - `unset RUSTFLAGS; cargo check`：exit 0。
  - `cargo test --lib acp::tests:: --no-run`：exit 0，测试二进制编译成功。
  - `git diff --check`：exit 0。
- 必测步骤：
  1. 使用可控 fake ACP 子进程，收到 initialize 后返回 JSON-RPC error，再连续调用 `AcpClient::connect` 5 次。
  2. 每次失败后检查 fake 子进程 PID 已退出，且不存在累积进程。
  3. fake ACP 不响应 initialize，等待 30 秒 RPC timeout，确认进程被 kill + wait。
  4. fake ACP 正常 initialize 后 Drop client，确认子进程退出。
- 验收：initialize error/timeout、pipe 获取失败、正常 client Drop 均不遗留子进程；cleanup 错误进入日志。
- 测试结果填写区：待 fake ACP 进程级验收。

### BUG-P1-04

- 状态：待测试
- 根因：原 `switch_agent/reconnect_agent` 在连接新 Agent 前先 kill 旧 client。目标 exe 无效或 initialize 失败时，旧 Agent 已不可用，但 activeAgent 与 sessions 仍指向旧状态，应用进入瘫痪状态。
- 实现摘要：新增独立 `agent_lifecycle` 锁；switch/reconnect 先完成新 `AcpClient::connect`，仅成功后调用 `replace_agent_client` 原子替换。swap 后提交 activeAgent（switch）、清空旧 sessions、重启 notification dispatcher，再 kill+wait 旧 client。失败发生在 swap 前，不修改旧状态。
- 修改文件：`src-tauri/src/lib.rs`、后端待测清单、后端交接文档。
- 对照 Peri 真值：`AcpClient::connect` 只有在 Peri initialize response 成功后返回可用 client；因此该返回值是两阶段切换的 prepare 完成边界。
- 并发/生命周期不变量：switch/reconnect 互斥；旧 client 在新 client initialize 完成前保持可用；activeAgent/sessions 仅在 swap 成功后提交；旧 child 在 swap 后 kill+wait。
- 自动验证：`unset RUSTFLAGS; cargo check` exit 0；`git diff --check` exit 0。
- 未验证：无效 exe/initialize error 下旧 Agent 继续 prompt；成功切换真实进程顺序。
- 剩余风险：当前其他 ACP 操作未捕获 generation；成功 swap 时旧 operation 的 stale response/notification 隔离属于 BUG-P1-06。
- commit：未提交。

### BUG-P1-06

- 状态：待测试
- 根因：`load_persisted_session` 在旧 ACP client 上等待 response 时，成功 switch/reconnect 会替换 client 并清空 sessions；旧 load 随后仍可能返回成功，形成“command 成功但 session 映射已不存在”。原错误回滚还会无条件按 source 删除，可能误删新代同名映射。
- 实现摘要：AppState 增加 `AtomicU64 client_generation`；每次成功 client swap 后递增。load 开始捕获 generation，response 后必须通过 `ensure_generation`；stale 时返回明确错误，并只在 `source` 仍指向本次 `periId` 时回滚。RPC error 路径同样条件删除。
- 修改文件：`src-tauri/src/lib.rs`、后端待测清单、后端交接文档。
- 并发/生命周期不变量：generation 仅在成功 swap 后变化；失败切换不使进行中操作 stale；旧 generation response 不提交成功；回滚不得删除新 generation 映射。
- 自动验证：`unset RUSTFLAGS; cargo check` exit 0；`git diff --check` exit 0。
- 未验证：fake ACP 阻塞 load 与成功 switch/reconnect 并发时序。
- 剩余风险：set/mode/cancel/close/send 尚未统一捕获 generation；当前提交只处理 BUG-P1-06 明确验收的 load 竞态根因。
- commit：未提交。

### BUG-P1-09

- 状态：待测试
- 根因：Pylon 只检查 JSON-RPC error。Peri stdio 在 ThreadStore create 失败时返回成功 envelope + `sessionId="error"`；prompt response 缺失/取消/未知 stopReason 也会被当成正常完成，导致伪 session、错误提交首次 prompt 状态和 `peri:done`。
- 实现摘要：强化 `session_id_from`，拒绝缺失、空白和 sentinel error。新增 `prompt_stop_reason`，仅接受 `end_turn/max_turn_requests`；cancelled、缺失和未知值返回 Err。send_message 在提交 `has_first_prompt` 和 emit done 前校验业务 response，失败 emit `peri:error`。
- 修改文件：`src-tauri/src/acp.rs`、`src-tauri/src/lib.rs`、后端待测清单、后端交接文档。
- 对照 Peri 真值：`acp_stdio/session/create.rs` 的 ThreadStore error sentinel；`acp_stdio/session/prompt.rs` 的 unknown-session EndTurn；`prompt_exec.rs` 的 PromptResponse stop reason 映射。
- 新增测试：`rejects_empty_and_error_session_ids`、`validates_prompt_stop_reasons`。
- 自动验证：`unset RUSTFLAGS; cargo check` exit 0；`cargo test --lib acp::tests:: --no-run` exit 0；`git diff --check` exit 0。
- 未验证：fake ACP 业务失败事件与前端 emit 路径。
- 剩余风险：Peri unknown-session EndTurn 与真实零输出 EndTurn 的 response 完全同形。Pylon不修改 Peri，无法从 response 单独消除该协议歧义；依靠本地映射与 generation 避免未知 session 请求。
- commit：未提交。

### BUG-P1-08

- 状态：待测试
- 根因：send_message 只把附件路径转成 `[Attached: 文件名]` 文本，`prepare_prompt` 固定只构造一个 text block；Peri 从未收到文件内容。
- 实现摘要：新增 `prompt_blocks`。后端读取附件、限制最多8个/单文件10MiB；UTF-8文本转 text block；PNG/JPEG/GIF/WebP 转带 `mimeType` 和 base64 `data` 的 image block；不支持 MIME、目录、缺失、超限和读取失败返回 Err。`prepare_prompt` 改为接受完整 block 数组。
- 修改文件：`src-tauri/src/acp.rs`、`src-tauri/src/lib.rs`、`src-tauri/Cargo.toml`、`src-tauri/Cargo.lock`、后端待测清单、后端交接文档。
- 对照 Peri 真值：`acp_stdio/session/prompt.rs:31-52` 仅消费 Text/Image，图片调用 `image_base64(mime_type,data)`，其余 block 当前忽略。
- 安全不变量：附件在 emit user/写 stdin 前完整验证；大小、数量、MIME、UTF-8均失败即停止；不支持类型不降级成文件名成功。
- 自动验证：`unset RUSTFLAGS; cargo check` exit 0；`git diff --check` exit 0。
- 未验证：真实文本逐字读取、图片 Agent 识别、全部错误分支的 Tauri UI 保留输入/附件行为。
- 剩余风险：前端字符串路径无法证明一定来自 dialog；需要未来 Tauri scoped capability 才能彻底约束任意路径。
- commit：未提交。

### BUG-P2-05

- 状态：待测试
- 根因：export 在 `session/load` 后固定 sleep 1500ms 猜测 replay 完成；大历史可能截断，小历史无谓等待。reader 原先先完成 pending response 再 broadcast 同一 response，使 load future 边界与 notification collector 排队关系不够明确。
- 实现摘要：reader 调整为先 broadcast 每条 RawMessage，再向 pending oneshot 提交 response。结合 Peri load 在 response 前 replay 的真值，load future 返回即证明所有 replay 已进入 broadcast。export 删除固定 sleep，response 后立即停止 collector。
- 修改文件：`src-tauri/src/acp.rs`、`src-tauri/src/lib.rs`、后端待测清单、后端交接文档。
- 并发/生命周期不变量：notification 入 broadcast happens-before response oneshot 完成；export 使用协议 response 边界，不使用时间猜测。
- 自动验证：`unset RUSTFLAGS; cargo check` exit 0；`git diff --check` exit 0。
- 未验证：大量 replay collector 完整性与 Lagged 路径。
- 剩余风险：markdown formatter 暂只导出 assistant chunks；user/reasoning/tool/metadata 和 Lagged 明确错误属于 BUG-P2-05 后续独立切片。
- commit：未提交。

### BUG-P0-04

- 根因：后端丢弃 load response，且前端无法区分 replay user event。
- 实现摘要：透传 `_meta.periReplay`；`load_session/load_persisted_session` 返回完整 response。
- 主要文件：`src-tauri/src/acp.rs`、`src-tauri/src/lib.rs`
- 对照真值：Peri `acp_stdio/session/create.rs` 与 `peri-acp/dispatch/session_replay.rs`。
- 静态验证：`cargo check` exit 0。
- 未验证：真实 replay notification、完整 modes/configOptions、跨 source 路由。

## 当前工作区注意事项

- BUG-P0-01 已提交：`32a2d70`。
- BUG-P1-05 已提交：`7a8a2eb`。
- BUG-P1-04 已提交：`a7c6e7d`。
- BUG-P1-06 已提交：`c2b52cb`。
- BUG-P1-09 已提交：`d8b9e56`。
- BUG-P1-08 已提交：`d13e117`。
- BUG-P2-05 replay 边界切片当前未提交。
- 不得恢复既有 Markdown 删除状态。
- `src-tauri/src/lib.rs` 混有多个已实施 Bug，后续 commit 必须精确拆分，禁止整体 stage。
- Rust test binary 曾在 Windows GNU/Tauri 环境报 `STATUS_ENTRYPOINT_NOT_FOUND (0xc0000139)`，未进入 test harness；不得写成测试通过。

## 下一入口

1. 验收 BUG-P2-05 大历史 replay 完成边界。
2. 继续 BUG-P2-05 formatter/Lagged 独立切片。
3. 再处理 BUG-P2-08 close retry。
4. 每次 Task 完成必须同步更新本文件与 `后端待测清单.md`。

## 最近 Task 记录

- Task：BUG-P2-05 replay 完成边界
- commit：未提交
- 剩余风险：尚未执行大历史 replay 集成验收；formatter 与 Lagged 尚未修复。
