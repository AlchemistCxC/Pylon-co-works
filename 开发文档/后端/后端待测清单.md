# Prism Desktop 后端待测清单

说明：每个后端 Task 完成时必须更新本文件。条目通过后保留证据，不直接删除。

## BUG-P0-01：Prompt timeout cancel 与按会话串行

- 状态：待测试
- 修改范围：`src-tauri/src/lib.rs`、`src-tauri/src/acp.rs`
- 本次实现：
  1. prompt timeout 后发送对应 `periId` 的 `session/cancel`。
  2. 保留原 oneshot receiver，最长等待 30 秒接收 Peri 最终 prompt response；等待期间同 source prompt lock 不释放。
  3. 30 秒内未收敛时移除当前 `source -> periId` 映射并 best-effort `session/close`，阻止同一未知远端 session 被立即复用。
  4. 修复 `call_async` 与 `prepare_prompt` 序列化失败时 pending 未删除的问题。
  5. 新增 timeout cancel/最终响应及正常响应不 cancel 两个 `tokio::test`。
- 自动验证：
  - `unset RUSTFLAGS; cargo check`：exit 0。
  - `git diff --check`：exit 0。
  - `cargo test --lib timeout_sends_cancel_and_waits_for_final_response -- --nocapture`：Cargo 编译成功，但测试二进制未进入 harness，进程状态 `0xc0000139 STATUS_ENTRYPOINT_NOT_FOUND`；shell exit 127，不得视为测试通过。
  - `cargo test --lib response_before_timeout_does_not_send_cancel -- --nocapture`：同上，shell exit 127。
- 必测步骤：
  1. 在可运行 Rust test harness 的环境执行新增两个单元测试。
  2. fake ACP 延迟 prompt 超过 timeout，抓取 `session/cancel` 并确认 `sessionId` 为目标 periId。
  3. cancel 后延迟返回 `stopReason=cancelled`，确认同 source 第二个 prompt 在最终 response 前不会写入。
  4. cancel 后不返回 response，确认 30 秒后本地映射移除、发送 `session/close`，重试创建新 Peri session。
  5. 对不同 source 并发 prompt，确认互不阻塞。
  6. 注入旧 prompt 迟发 chunk，验证前端事件层不会混入新 turn。
- 验收：同 session 不并发；不同 session 可并行；timeout 后 cancel；未收敛 session 不复用；pending 无残留；旧 turn 不污染新 turn。
- 已知限制：通知不携带 turn/request identity；30 秒内 cancel 收敛阶段的旧 chunk 仍会按原 source 发出。prompt lock map 尚未清理。
- 测试结果填写区：待真实 fake ACP/集成测试。

## BUG-P1-05：initialize 失败 child 清理

- 状态：已实施，待测试
- 修改范围：`src-tauri/src/acp.rs`
- 本次实现：新增 `ManagedChild` RAII guard。spawn 后接管 child；initialize RPC error/timeout、pipe 获取失败和其他 connect 失败出口自动执行 `try_wait`，仍运行则 `kill + wait`；成功连接后由 `AcpClient` 持有，显式 kill 与 Drop 使用同一清理逻辑。
- 自动验证：`unset RUSTFLAGS; cargo check` exit 0；`cargo test --lib acp::tests:: --no-run` exit 0；`git diff --check` exit 0。
- 必测步骤：可控 fake ACP 连续制造 initialize error/timeout，检查每次 PID 退出；正常 client Drop 后检查子进程退出。
- 验收：无孤儿子进程；cleanup 错误进入日志。
- 测试结果：待 fake ACP 进程级验收。

## BUG-P1-04：Agent 两阶段原子切换

- 状态：已实施，待测试
- 修改范围：`src-tauri/src/lib.rs`
- 本次实现：新增独立 `agent_lifecycle` 锁串行 switch/reconnect；先在旧 client 保持可用时完成新 client spawn+initialize；成功后通过 `replace_agent_client` 短临界区替换 `state.acp`，再提交 activeAgent/清空旧 sessions/重启 dispatcher，最后 kill+wait 旧 client。新连接失败时函数在 swap 前返回，旧 client、activeAgent、sessions 与 dispatcher 保持不变。
- 自动验证：`unset RUSTFLAGS; cargo check` exit 0；`git diff --check` exit 0。
- 必测步骤：目标 Agent 使用无效 exe 和 initialize error 两种失败；切换后验证旧 Agent 仍能处理 prompt、activeAgent 未变化、sessions 未清空。成功切换时验证新 client 先 initialize，再 swap，旧进程最后退出。
- 验收：失败不破坏旧 Agent；成功切换顺序为 connect+initialize → swap → 关闭旧 client；switch/reconnect 互斥。
- 已知限制：尚未引入 client generation，进行中的其他 ACP 操作与成功 swap 的 stale response 隔离属于 BUG-P1-06。
- 测试结果：待 fake ACP/真实集成验收。

## BUG-P1-06：load 与 Agent switch/reconnect generation 隔离

- 状态：已实施，待测试
- 修改范围：`src-tauri/src/lib.rs`
- 本次实现：AppState 新增原子 `client_generation`；每次成功 swap ACP client 后 generation +1。`load_persisted_session` 在建立临时路由前捕获 generation，load response 后校验；若期间发生 switch/reconnect，则返回明确 stale generation error，并且仅在映射仍指向本次 periId 时移除临时 source，避免删除新代映射。load RPC error 同样采用条件回滚。
- 自动验证：`unset RUSTFLAGS; cargo check` exit 0；`git diff --check` exit 0。
- 必测步骤：fake ACP 阻塞 load response，同时成功 switch/reconnect；放行旧 response 后确认 load 返回 stale error，旧临时映射被清理且不会删除新 Agent 的同 source 映射。
- 验收：不得出现旧 generation load 返回成功；stale 操作明确失败；回滚不破坏新 generation 状态。
- 已知限制：本 Task 当前只收敛审计指定的 load 提交竞态。set/mode/cancel/close/send 的完整 generation 扩展仍需后续生命周期验收后逐项实施，避免扩大当前根因范围。
- 测试结果：待 fake ACP 并发验收。

## BUG-P1-09：Peri 业务失败响应校验

- 状态：已实施，待测试
- 修改范围：`src-tauri/src/acp.rs`、`src-tauri/src/lib.rs`
- 本次实现：`session_id_from` 拒绝缺失、trim 后空字符串和大小写 sentinel `error`，不建立伪 session。新增 `prompt_stop_reason` 校验 prompt response 必须包含受支持的 `stopReason`；`cancelled`、缺字段和未知值走 `peri:error`/Err，不提交 `has_first_prompt`，不 emit 正常 `peri:done`。
- 对照 Peri 真值：stdio `session/new` ThreadStore 失败返回 `sessionId="error"`；未知 session prompt 返回 `stopReason=end_turn`；正常 prompt response 由 `PromptResponse::new` 返回 snake_case stopReason。
- 自动验证：`unset RUSTFLAGS; cargo check` exit 0；`cargo test --lib acp::tests:: --no-run` exit 0；`git diff --check` exit 0。
- 新增测试：拒绝空/error sessionId；接受 end_turn、拒绝 cancelled/缺失 stopReason。
- 必测步骤：fake ACP 返回 `sessionId:error`、空 ID、cancelled、缺失/未知 stopReason，确认不建映射、不提交首轮 persona、不 emit done。
- 验收：业务失败不会显示为成功；错误对用户可见；伪 session 不进入 sessions map。
- 已知限制：Peri stdio 对“未知 session”返回的合法 `end_turn` 与真实零输出成功在 response 结构上不可区分；在不修改 Peri 的前提下，Pylon只能依靠本地 source→periId 映射与 generation 防止发送未知 session。该协议歧义已记录，不伪造额外成功判据。
- 测试结果：待 fake ACP 运行时验收。

## BUG-P1-08：附件真实 ACP ContentBlock

- 状态：已实施，待测试
- 修改范围：`src-tauri/src/acp.rs`、`src-tauri/src/lib.rs`、`src-tauri/Cargo.toml`、`src-tauri/Cargo.lock`
- 本次实现：移除仅拼接 `[Attached: filename]` 的伪附件。后端读取真实文件并构建 ACP prompt blocks：UTF-8 文本作为 text block；PNG/JPEG/GIF/WebP 作为 image block，使用真实 MIME 与 base64 data。单文件限制 10 MiB、单次最多 8 个；目录、读取失败、非 UTF-8 未知文件和不支持 MIME 返回明确 Err，且发生在 `peri:user` emit/ACP 写入前。
- 对照 Peri 真值：`acp_stdio/session/prompt.rs` 直接支持 Text 与 Image ContentBlock，image 读取 `mime_type/data`；Audio/ResourceLink/Resource 当前被忽略，因此后端不伪装为成功。
- 自动验证：`unset RUSTFLAGS; cargo check` exit 0；`git diff --check` exit 0。
- 必测步骤：附件纯文本并核对 Agent 能逐字读取；附件 PNG/JPEG 并抓取 JSON-RPC image block；测试 >10MiB、目录、PDF/ZIP、缺失文件、超过8个附件，确认明确失败且不发送 prompt。
- 验收：附件内容而非文件名进入 Peri；MIME/大小限制生效；失败用户可见；不支持类型不显示为成功。
- 已知限制：路径仍来自前端 Tauri dialog 的字符串，当前 command 无不可伪造的 capability token；后端通过文件类型、大小、数量与读取结果限制风险，但任意路径信任边界仍需 Tauri scoped FS/capability 方案才能完全收敛。
- 测试结果：待真实 Tauri + Peri 集成验收。

## BUG-P2-05：导出 replay 完成边界

- 状态：已实施，待测试
- 修改范围：`src-tauri/src/acp.rs`、`src-tauri/src/lib.rs`
- 本次实现：ACP reader 先把每条消息送入 broadcast，再把 response 送入对应 pending oneshot。由此 `session/load` future 返回时，Peri 在 response 前发送的全部 replay notification 已进入 broadcast；export 移除固定 1500ms sleep，直接以 load response 为确定性完成边界后停止 collector。
- 对照 Peri 真值：`acp_stdio/session/create.rs::handle_load` 在 responder 前依次 replay history、发送 config update，随后才发送 LoadSessionResponse。
- 自动验证：`unset RUSTFLAGS; cargo check` exit 0；`git diff --check` exit 0。
- 必测步骤：构造大量 replay，确认 response 前的全部 broadcast 消息被 collector 接收；验证不再等待固定1.5秒；检查 user/assistant/reasoning/tool/metadata 导出完整性。
- 验收：response 是唯一 replay 边界；无固定 sleep；不静默截断。
- 已知限制：无固定 sleep；formatter 已覆盖 user/assistant/reasoning/tool/metadata；broadcast Lagged/Closed 会返回明确错误。仍需大历史真实集成验收确认 collector 调度完整性。
- 测试结果：待大历史 replay 集成验收。

## BUG-P2-08：close_session 远端失败可重试

- 状态：已实施，待测试
- 修改范围：`src-tauri/src/lib.rs`
- 本次实现：先读取 `source → periId`，在 ACP `session/close` 成功后才删除本地映射；远端 close RPC error、timeout 或连接失败都会保留映射并向调用方返回 Err，可重试。使用短生命周期 session creation lock，避免 close 与 session 创建/替换并发破坏映射。
- 对照 Peri 真值：`session/close` 是 request，必须等待 response；stdio handler 只有 responder 完成后才认为 close 请求结束。
- 自动验证：`unset RUSTFLAGS; cargo check` exit 0；`git diff --check` exit 0。
- 必测步骤：fake ACP 首次 close 返回 RPC error，确认 source/periId 映射仍在；第二次 close 成功后确认映射删除；close 与新建/替换同 source 并发时确认不删新映射。
- 验收：远端失败可重试；成功后本地删除；不会丢失 session 映射。
- 测试结果：待 fake ACP 运行时验收。

## BUG-P2-09：启动 Agent 失败不阻断 UI

- 状态：已实施，待测试
- 修改范围：`src-tauri/src/agent_config.rs`、`src-tauri/src/acp.rs`、`src-tauri/src/lib.rs`
- 本次实现：
  1. `agent_config::load` 和默认 Agent 选择改为 `Result/Option`，配置解析失败、空 registry 不再 panic。
  2. 新增显式 disconnected `AcpClient`。默认 Agent spawn/initialize 失败时保留错误诊断并继续启动 Tauri UI；后端命令通过既有 crashed/closed 语义返回错误。
  3. `reload_agents` 传播配置错误，不覆盖当前 registry。
  4. 删除 Agent 启动链和 Tauri run 尾部的 `expect`。
- 自动验证：
  - `unset RUSTFLAGS && cargo check`：exit 0。
  - `cargo test --lib agent_config::tests:: --no-run`：exit 0，测试二进制编译成功。
  - `cargo test --lib agent_config::tests:: -- --nocapture`：未进入 test harness，进程状态 `0xc0000139 STATUS_ENTRYPOINT_NOT_FOUND`，shell exit 127，不得视为测试通过。
  - `npm run build`：exit 0；仅有既存 dynamic import 与 chunk size warning。
  - `git diff --check`：exit 0。
- 必测步骤：
  1. 将默认 Agent exe 指向不存在路径，启动应用，确认主窗口仍出现且 stderr 含 `Pylon ACP agent unavailable`。
  2. 调用 `agent_status`，确认 `crashed=true`；发送消息返回明确错误，不导致应用退出。
  3. 修复 Agent 配置后调用 `reconnect_agent`，确认切换到 connected 并可正常 new/prompt。
  4. 构造空 agents registry，确认 UI 仍启动、`list_agents` 返回空列表、reconnect 返回 `no active agent configured`。
- 验收：默认 Agent 启动失败不 panic；UI 可用；状态/错误可查询；修复环境后可 reconnect 自愈。
- 已知限制：当前前端尚未接入 `agent_status` / `peri:agent-status`，因此 connected/error/reconnecting 的可视化仍是前端依赖；本 Task 不修改 `src/**`。
- 测试结果：待失效 exe + 真实 Tauri 启动验收。

## BUG-P3-02：后端事件错误不再静默吞掉

- 状态：已实施，待测试
- 修改范围：`src-tauri/src/lib.rs`
- 本次实现：统一通过 `emit_event` 派发 Tauri 事件，所有 emit 失败进入 warn 日志；窗口标题设置失败同样记录诊断，不再使用 `.ok()`/`let _ =` 静默丢弃。
- 自动验证：`unset RUSTFLAGS && cargo check` exit 0；`git diff --check` exit 0。
- 验收：关闭/销毁窗口期间制造 emit 失败，日志包含事件名与具体错误；正常事件链行为不变。
- 已知限制：pet 状态更新的 poisoned-lock 路径仍属于内部非关键 best-effort，本 Task 未扩大到前端 `.catch(() => {})`。
- commit：`a897dbe`。

## BUG-P3-03：运行时 reload Agent 配置

- 状态：已实施，待测试
- 修改范围：`src-tauri/src/agent_config.rs`、`src-tauri/src/lib.rs`
- 本次实现：新增 `load_from_path`；`reload_agents` 必须接收显式 `configPath` 或读取 `PYLON_AGENTS_CONFIG`，运行时读取文件并仅在解析成功后替换 registry，不再重复读取编译期 `include_str!`。
- 自动验证：`unset RUSTFLAGS && cargo check` exit 0；`git diff --check` exit 0；新增测试二进制可编译，当前 Windows harness 限制仍待可运行环境执行。
- 验收：修改外部 YAML 后调用 reload，`list_agents` 立即返回新 registry；无路径或 YAML 错误时返回明确 Err，旧 registry 保持不变。
- commit：`55e4498`。

## BUG-P3-04：agents.yaml 可移植路径

- 状态：已实施，待测试
- 修改范围：`agents.yaml`、`src-tauri/src/agent_config.rs`、`src-tauri/src/acp.rs`、`src-tauri/src/lib.rs`
- 本次实现：移除仓库 YAML 中开发机绝对路径；Agent 可执行文件默认按 PATH 查找。设置 `PYLON_AGENTS_CONFIG` 时，相对 exe（含目录）和 cwd 明确以配置文件所在目录为基准解析；reload 同样复用显式配置路径。
- 自动验证：
  - `unset RUSTFLAGS && cargo check`：exit 0。
  - `cargo test --lib agent_config::tests:: --no-run`：exit 0，测试二进制编译成功。
  - `npm run build`：exit 0；3306 modules transformed，仅有既存 dynamic import 与 chunk size warning。
  - `git diff --check`：exit 0。
- 验收：将配置目录整体移动后，相对 exe/cwd 仍解析到新目录；PATH 中的 `peri` 可直接启动；不存在路径返回明确 spawn 错误并保持 UI 降级运行。
- 已知限制：默认 `peri` 需要安装到 PATH；自包含发行包应通过外部配置写 `bin/peri.exe` 并设置 `PYLON_AGENTS_CONFIG`。
- 测试结果：待便携目录和真实进程验收。

## BUG-P1-06B：cancel 跨 Agent generation 隔离

- 状态：已实施，待测试
- 修改范围：`src-tauri/src/lib.rs`
- 本次实现：cancel 开始捕获 client generation，notification 写入后校验 generation；期间发生成功 switch/reconnect 时返回 stale error，不把旧 Agent cancel 误报为当前 Agent 成功。
- 自动验证：`unset RUSTFLAGS && cargo check` exit 0；`git diff --check` exit 0。
- 验收：阻塞 cancel 写入并切换 Agent，旧操作必须返回 stale generation error。
- commit：`adc43d7`。

## BUG-P1-06C：mode/config 跨 Agent generation 隔离

- 状态：已实施，待测试
- 修改范围：`src-tauri/src/lib.rs`
- 本次实现：`set_mode` 与 `set_config_option` 在请求前捕获 generation、response 后校验，旧 Agent 的配置响应不得提交为当前 Agent 成功。
- 自动验证：`unset RUSTFLAGS && cargo check` exit 0；`git diff --check` exit 0。
- 验收：阻塞 mode/config response 并成功切换 Agent，旧调用返回 stale error；前端乐观状态按既有逻辑回滚。
- commit：`f0e735b`。

## BUG-P1-06D：persisted session list 跨 generation 隔离

- 状态：已实施，待测试
- 修改范围：`src-tauri/src/lib.rs`
- 本次实现：`list_persisted_sessions` 捕获 generation 并在 response 后校验，避免旧 Agent 的 ThreadStore 列表覆盖当前 Agent 视图。
- 自动验证：
  - `unset RUSTFLAGS && cargo check`：exit 0。
  - `npm run build`：exit 0；3307 modules transformed，仅有既存 Vite warnings。
  - `git diff --check`：exit 0。
- 验收：阻塞 list response 并切换 Agent，旧列表返回 stale error，不进入前端列表。
- 测试结果：待 fake ACP 并发验收。

## BUG-P1-06E：显式新建会话 generation 隔离

- 状态：已实施，待测试
- 修改范围：`src-tauri/src/lib.rs`
- 本次实现：`new_session` 在 ACP 请求前捕获 generation，response 后校验，旧 Agent 创建结果不得写入当前 sessions map。
- 自动验证：`unset RUSTFLAGS && cargo check` exit 0；`git diff --check` exit 0。
- 验收：阻塞 session/new response 并成功切换 Agent，旧调用返回 stale error且不建立映射。
- commit：`dd3cca5`。

## BUG-P1-06F：隐式新建会话 generation 隔离

- 状态：已实施，待测试
- 修改范围：`src-tauri/src/lib.rs`
- 本次实现：`send_message` 缺少 session 时的隐式 session/new 同样在 response 后校验 generation，旧 Agent periId 不进入当前 sessions map。
- 自动验证：`unset RUSTFLAGS && cargo check` exit 0；`git diff --check` exit 0。
- 验收：首条消息建会话期间切换 Agent，旧 new response 返回 stale error，不继续发送 prompt。
- commit：`c225b4b`。

## BUG-P1-06G：prompt completion generation 隔离

- 状态：已实施，待测试
- 修改范围：`src-tauri/src/lib.rs`
- 本次实现：prompt 准备前捕获 generation；有效 response 后、提交 `has_first_prompt` 和 emit `peri:done` 前校验。旧 Agent prompt completion 不得修改当前 session 或显示正常完成。
- 自动验证：
  - `unset RUSTFLAGS && cargo check`：exit 0。
  - `npm run build`：exit 0；3307 modules transformed，仅有既存 Vite warnings。
  - `git diff --check`：exit 0。
- 验收：阻塞 prompt response 并切换 Agent，旧 response 返回 stale error；不提交首轮状态、不 emit done。
- 测试结果：待 fake ACP 并发验收。

## BUG-P0-04：load response 与 replay 标记

- 状态：待测试
- 类型：跨端
- 修改范围：`src-tauri/src/lib.rs`、`acp.rs`
- 已完成静态验证：`unset RUSTFLAGS; cargo check` exit 0。
- 必测步骤：
  1. 捕获 `session/load` 前的 replay notifications。
  2. 验证 replay update 的 `_meta.periReplay=true` 被转换到前端事件。
  3. 验证 Tauri command 返回完整 `modes/configOptions` response。
- 验收：response 是确定性 replay 边界；配置完整返回；unknown source 不错误路由。
- 测试结果：待填写。

## BUG-P1-01：空 Session Prompt 回退 Profile persona

- 状态：待测试
- 修改范围：`src-tauri/src/lib.rs`
- 必测输入：`None`、空字符串、纯空格、非空 Session Prompt。
- 验收：前三者回退 Profile persona；非空值覆盖 persona。
- 测试结果：待填写。

## BUG-P1-02：首轮 prompt 状态提交

- 状态：待测试
- 修改范围：`src-tauri/src/lib.rs`
- 必测失败：stdin 关闭、JSON-RPC error、timeout、cancel、Agent 崩溃。
- 验收：失败后重试仍注入 persona；有效 response 后才提交首轮状态。
- 依赖：BUG-P1-09 必须排除业务假成功。
- 测试结果：待填写。

## BUG-P1-03：prompt 等待不占用全局 creation lock

- 状态：待测试
- 修改范围：`src-tauri/src/lib.rs`
- 必测步骤：A 长时间生成时，B 发送、新建 C，并对 A 发第二个 prompt。
- 验收：B/C 不被 A 阻塞；A 的两个 prompt 串行。
- 测试结果：待填写。
