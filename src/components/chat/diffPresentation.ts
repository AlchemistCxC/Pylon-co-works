/**
 * diffPresentation — 兼容 re-export（P1-10）。
 *
 * diff 纯解析真实定义已迁入 `domains/tool/diffPresentation.ts`；本文件仅转发导出，
 * 供既有组件 import 面不爆炸。确认无消费者后可删除。
 */

export * from '../../domains/tool/diffPresentation.ts'
