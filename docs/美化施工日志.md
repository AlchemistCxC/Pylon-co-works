# Pylon 前端美化施工日志

## 交接状态（2026-08-07，B2-P3 完成 / 宠物事件姿态接线准备）

- 阶段 A 与 B1 已完成；宠物重绘已完成 B2-P0 动态 Preview、B2-P1 五阶段生产本体、B2-P2 13 mood 表情姿态、B2-P3 六步跑动与左右朝向。
- 当前生产宠物使用 36×32 crispEdges 画布、五阶段独立轮廓、短胳膊/爪尖、稳定 SVG layer class；显示 shell 仍为 116×106，不扩大工作区遮挡。
- B2-P3 已新增 `PetDirection`，漫游目标与水平拖拽会更新朝向；左向使用 `--pet-facing:-1`，所有呼吸、poke、hop、code-eating、tablet 动画均保留朝向。
- 跑动采用 720ms / 6-step 离散周期：身体前倾/落地压缩、阴影宽度变化、尾巴反摆、耳/触角惯性、左右臂腿反相、luminary 光翼惯性；位置迁移仍沿用原 2.2s shell transition。
- 下一任务：`B2-P4` Rust 事件 → visual pose 映射。先定义纯前端 pose 映射与 class/data 属性，再让现有 Code/Read/Execute/Network/AgentSpawn/ToolCancelled/Timeout/Refused/Maxed/Connected/Crashed/Feed/Play/Poke 消费，不先接 cosmetics/HUD。
- B2-P5 之后才补 v2 contracts normalize + MOCK_PET；B2-P6 接 11 件 cosmetics；B2-P7 衣橱/needs/crafting/day_part；B2-P8 真实 Tauri invoke/轮询/装备验收。成就面板仍不做。
- 本轮未修改 `src-tauri/`，未新增依赖或 ThemeSettings 字段；现有 DTO、漫游/拖拽、code-eating 状态机、tablet coding 与 invoke 链路保留。
- 最新 fresh 验证：`npx tsc --noEmit` 绿；`npm run test` 6 files / 20 tests 绿；`npm run lint` 0 error；`npm run build` 3483 modules 绿；`git diff --check` 绿。
- B2-P3 浏览器硬证据：36×32 viewBox；right transform `matrix(0.88,0,0,0.88,0,0)`，left transform `matrix(-0.88,0,0,0.88,0,0)`；`pet-run-body` / `pet-limb-a` 实际运行并发生帧变化；无横向溢出；控制台 0 error。
- 工作区仍有其他 Lane 的既有未提交改动；不要 reset、不要整仓 stage。根目录 `pylon-0.1.0-source.zip` 仍为原有未跟踪文件，不要纳入提交。

> 每个视觉小任务完成后追加一行；状态：完成 / 阻塞 / 跳过。

```text
A1-01 探索诊断与截图留档 | 完成 | 依赖：D-08 | 验证：浏览器逐面探索 + 10 张截图留档 + 视觉综合分析 | 发现 Overview、Agent、FileSheet、Gateway、Runtime、Browser、History、Search、Prism、Settings 以及侧栏/右栏/顶栏的层级、密度、状态和跨面一致性问题；范围外功能问题仅记备注
A2-01 统一设计规范 | 完成 | 依赖：A1-01 | 验证：docs/美化设计规范.md 落盘 + 零新主题字段方案 | 确立 4/8/12/16/24/32/48 间距、4/6/8/12 圆角、28/36/40 控件高度、统一状态与 ccVariant 约定；FileSheet 活动栏图标化列为建议，未实施
A3-01 Overview 门面与全局 token | 完成 | 依赖：A2-01 | 验证：tsc 绿 + 全量前端守卫绿（其中既有 E-12A blocked 记录）+ build 绿 + git diff --check 绿 + 浏览器硬证据：3 卡片各约 349.33×190，无横向溢出 | 修改 src/index.css、src/sheets/OverviewSheetView.css、src/sheets/OverviewSheetView.tsx；新增 CSS 内部 token，不进入 ThemeSettings；补充首屏 lede、编号、统一卡片/按钮/focus/响应式样式
A3-02 顶栏、Sheet Tab 与 Launcher 统一控件尺度 | 完成 | 依赖：A3-01 | 验证：tsc 绿 + 全量前端守卫绿（其中既有 E-12A blocked 记录）+ build 绿 + git diff --check 绿 + 浏览器硬证据：Launcher input 56px、item 52px；Sheet Tab 184×37px、6px 圆角、无横向溢出；控制台仅既有 favicon 404 | 修改 src/App.css；复用 A3-01 CSS token，统一顶栏按钮、窗口控制、Sheet Tab active/focus/close、Launcher 输入区/列表项/footer 的尺度与状态；未改逻辑、数据或依赖
A3-03 侧栏与右栏统一搜索控件、列表行、selected 状态 | 完成 | 依赖：A3-02 | 验证：tsc 绿 + 全量前端守卫绿（其中既有 E-12A blocked 记录）+ build 绿 + git diff --check 绿 + 浏览器硬证据：侧栏搜索 189×36px、会话行约 233×52px；右栏 280px、header 48px、mode 28px、搜索 255×38px；无横向溢出；控制台仅既有 favicon 404 | 修改 src/components/Sidebar.css、src/components/right-panel/ContextPanel.css、src/components/right-panel/AgentContextPanel.tsx、src/components/right-panel/FileContextPanel.tsx、src/components/chat/MessageSearchBar.css；统一搜索 focus ring、列表 active accent 锚点、右栏 mode/header/空态/关联文件行；补齐 Agent/File 右栏消费 ContextPanel CSS；未改逻辑、数据或依赖
A3-04a Runtime 混合型密度基准 | 完成 | 依赖：A3-03；拍板：C 混合型 | 验证：tsc 绿 + 全量前端守卫绿（其中既有 E-12A blocked 记录）+ build 绿 + git diff --check 绿 + 浏览器硬证据：筛选控件/清空按钮 225×36px、诊断 chip 高 24px pill、日志 header 36px、日志行 30px、详情 1166×52px/6px 圆角；无横向溢出；控制台仅既有 favicon 404 | 修改 src/sheets/RuntimeSheetView.tsx、src/sheets/RuntimeSheetView.css；左栏/诊断区采用轻量产品型层级，日志流保持紧凑专业型；新增纯展示 header 与双端计数，不改过滤、监听、clear、展开逻辑或数据契约
A3-04b Gateway 管理面视觉层级 | 完成 | 依赖：A3-04a；拍板：沿用轻量产品型 section + 配置卡片 | 验证：tsc 绿 + 全量前端守卫绿（其中既有 E-12A blocked 记录）+ build 绿 + git diff --check 绿 + 浏览器硬证据：适配器列表、路由卡片、写回表单、Prism 只读注入区均建立统一 section/控件层级；前后截图留档 docs/美化截图/A3-04b-gateway-before.png、A3-04b-gateway-after.png；无新增主题字段、无逻辑/数据契约改动
A3-04c History + Search 管理面统一 | 完成 | 依赖：A3-04b | 验证：tsc 绿 + 全量前端守卫绿（其中既有 E-12A blocked 记录）+ build 绿 + git diff --check 绿 + 浏览器硬证据：History 6 条存档统一为 40px+ 管理列表行，回放/导出状态分层；Search 补齐 sheet header、说明、40px 搜索框与结果/空态规范；前后截图留档 docs/美化截图/A3-04c-*-before.png、A3-04c-*-after.png | 修改 HistorySheet.css、SearchSheet.css、SearchSheetView.tsx；仅新增展示标题结构与视觉样式，搜索、回放、导出逻辑不变，零新主题字段
A3-04d Prism 管理面视觉统一 | 完成 | 依赖：A3-04c | 验证：tsc 绿 + demoPanels 守卫绿 + 全量前端守卫绿 + build 绿 + git diff --check 绿 + 浏览器硬证据：Prism 左导航、演示禁用提示、Bots 卡片、World Book 表格统一为专家型工作台；前后截图留档 docs/美化截图/A3-04d-prism-before.png、A3-04d-prism-after.png、A3-04d-prism-worldbook-after.png | 修改 src/components/PrismSheet.css；仅视觉层改动，保留演示禁用行为、Tab 状态与现有管理结构，零新主题字段
A3-04d-fix Prism 左栏尺寸统一 | 完成 | 依赖：A3-04d | 验证：修正后 fresh tsc + 全量前端守卫 + build + diff check 绿；Prism `.ps-nav` 改为消费 `--sheet-sidebar-width`，与 File/Gateway/Runtime 左栏共享同一宽度 token；不改结构与行为
A4-01 Agent 消息区精致化 | 完成 | 依赖：A3-04d-fix | 验证：tsc 绿 + 全量前端守卫绿（其中既有 E-12A blocked 记录）+ build 绿 + git diff --check 绿 + 浏览器硬证据：用户行 accent 轨道、assistant copy/focus、tool hover/body、reasoning 展开、任务树状态、spinner/replay/回底控件统一；前后截图留档 docs/美化截图/A4-01-agent-before.png、A4-01-agent-after.png | 修改 src/components/chat/ChatView.css；仅追加视觉覆盖层，保留消息渲染、状态机、动画和主题字段
A4-02 TaskTree / spinner / replay bar / 中控状态控件统一 | 完成 | 依赖：A4-01 | 验证：tsc 绿 + 全量前端守卫绿（其中既有 E-12A blocked 记录）+ build 绿 + git diff --check 绿 + 浏览器硬证据：TaskTree 与中控 tasks pill 使用同一 TASKS 标识和 28px compact 尺度；spinner 保持无框轻量活动行，replay bar、状态栏 widget 间距和 context ring 尺度统一；前后截图留档 docs/美化截图/A4-02-status-controls-before.png、A4-02-status-controls-after.png | 修改 src/components/chat/ChatView.css、src/components/ControlCenter.css；仅视觉覆盖，不改任务桥、spinner 状态机、中控 registry 或布局数据；零新主题字段
A4-03 FileSheet 编辑器工作面精致化 | 完成 | 依赖：A4-02 | 验证：npx tsc --noEmit 绿 + 全量前端守卫绿（其中既有 E-12A blocked 记录）+ npm run build 绿 + git diff --check 绿；浏览器硬证据：FileSheet 空态建立编辑器网格/引导卡，活动栏改为紧凑 glyph + tooltip，文件树行/文件夹文件标识/hover/focus、tab active/close、gutter/改动行、dispatch bar、status bar 与 Git diff 统一；语法高亮继续消费现有 starry-night + syn/editor 主题变量，框选行号和回传会话链路保留并补充状态提示；前后截图留档 docs/美化截图/A4-03-filesheet-before.png、docs/美化截图/A4-03-filesheet-after.png | 修改 src/sheets/file/FileSheet.css、FileSheetSidebar.tsx、FileSheetView.tsx、FileTree.tsx；仅视觉相关 JSX/CSS，未改 FileSheet reducer/store/事件契约/src-tauri/，零新主题字段
A5-01 全局交互状态补全 | 完成 | 依赖：A4-03 | 验证：tsc + 全量守卫 + build + diff check 绿；浏览器硬证据：Sheet Tab 键盘 focus 实际为 2px accent ring；Gateway/Runtime 展开态、Sidebar 会话行、PermissionDialog、InputBar disabled/selected/focus 统一 | 修改 src/index.css、Sidebar.css、InputBar.css、PermissionDialog.css、RuntimeSheetView.css、GatewaySheet.css/GatewaySheetView.tsx；零新主题字段，不改业务行为
A5-02 Browser / File / Search 空态统一 | 完成 | 依赖：A5-01 | 验证：tsc + 全量守卫 + build + diff check 绿；Browser 与 Search 浏览器 DOM 验证通过 | Browser 建立角色骨架，Search 增加初始/无结果引导，File 未选择 source/空目录使用紧凑工作区空态；后续 Browser 契约变化由 A5-03 覆盖
A5-03 Browser Tauri 子 WebView 工作区 | 完成（待真实 Tauri 运行验收） | 依赖：后端 commit afe6275 | 验证：fresh tsc 绿 + npm test 6 files/20 tests 绿 + npm run lint 0 error/0 warning + npm run build 3482 modules 绿 + diff check 绿；浏览器 DOM 硬证据：1920×889 下左栏 184px、主区约1736px，折叠后左栏48px、主区约1872px；仅既有 favicon 404 | 修改 BrowserSheetView.tsx、BrowserSheet.css、test-browser-state.mts；接 browser_start(bounds)/status/page/navigate/back/forward/reload/set_bounds/close，ResizeObserver 同步 WebView 区域，Sheet 卸载 close；左栏工具为历史/书签/下载/控制台，仅入口视觉，具体工具数据/行为未接；下一接手必须在真实 Tauri Windows 验证 WebView2 bounds、Sheet 切换遮挡与关闭回收
B1-01 Pylon 品牌标记接入 | 完成 | 依赖：A2 风格基调 | 验证：npm test 6/20 + lint + build 3483 + diff check 绿；浏览器 Overview/顶栏无溢出 | commit 23c8e04；新增 PylonMark，接入 Overview 与 WorkspaceTitlebar；零新主题字段
B1-02 Search 品牌空态与几何精修 | 完成 | 依赖：B1-01 | 验证：npm test 6/20 + lint + build 3483 + diff check 绿；Search 初始空态 44px 锚点、3 节点、0 横向溢出 | commit 5d07d4d；Search 初始/无结果空态复用 PylonMark；File/Browser 保留各自语义 glyph
B1-03 品牌外框定稿 | 完成 | 依赖：B1-02；用户拍板 | 验证：npm test 6/20 + lint + build 3483 + diff check 绿；内部三边约 24/24/24 | commit 794dce7；恢复原六边形外框，内部保持正三角对称三节点与旧比例
B2-01 Cosmetics 第一批 Preview | 完成（仅 Preview） | 依赖：B1 完成 | 验证：8 card/8 pet、0 溢出、0 console error；fresh test/lint/build 绿 | docs/美化预览/B2-01-pet-cosmetics-preview.html；帽饰/披风/光效方向获用户认可，未接生产
B2-02 宠物 v2 UI 资产 Preview | 完成（仅 Preview） | 依赖：docs/宠物系统契约解析.md | 验证：11 cosmetics、3 needs、4 day_part、1 crafting、0 溢出、0 console error；fresh test/lint/build 绿 | docs/美化预览/B2-02-pet-v2-assets-preview.html；绘制 11 件契约装扮、HUD/crafting/day_part；未接生产
B2-03 Rust 宠物协议与本体重绘方向盘点 | 完成（设计/交接） | 依赖：直接读取 pet-core Rust 源码 | 验证：源码链与浏览器当前宠物实况核对 | 确认 4 HSM、13 mood、5 stage、事件动作；判定旧 24×22 本体与上下跳 walking 不足；下一步 B2-P0 做五阶段 + idle/run 动态 Preview
B2-P0 五阶段本体 + idle/run 动态 Preview | 完成（用户认可） | 依赖：B2-03 | 验证：5 stage/5 run 行、36×32 crispEdges、无横向溢出、方向翻转与暂停控制有效、控制台 0 error；fresh test 6 files/20 tests + lint + build 3483 + diff check 绿 | docs/美化预览/B2-P0-pet-five-stages-motion-preview.html；五阶段采用独立轮廓并补充短胳膊/爪尖，动画只用 SVG group + CSS steps()；未改生产组件
B2-P1 五阶段静态本体生产重绘 | 完成 | 依赖：B2-P0 用户认可 | 验证：npx tsc --noEmit + npm test 6 files/20 tests + npm run lint + npm run build 3483 modules + git diff --check 全绿；浏览器生产硬证据：seed 使用 36×32 viewBox、2 arm/2 leg/1 core、SVG 实际约 84.48×77.44、shell 116×106、pet-arm 动画实际变化、0 横向溢出、0 console error | 修改 src/components/PetCompanion.tsx、PetCompanion.css；五阶段独立轮廓、小胳膊与稳定 layer class 落入生产，保留现有 DTO、漫游/拖拽、行为状态机与事件流；下一步 B2-P2 补 idle/sleep 与 13 mood 表情姿态
B2-P2 idle/sleep 与 13 mood 表情姿态 | 完成 | 依赖：B2-P1 | 验证：npx tsc --noEmit + npm test 6 files/20 tests + npm run lint + npm run build 3483 modules + git diff --check 全绿；生产 DOM 逐态硬证据：13 mood 全覆盖，sleepy/tired 闭眼+Z、error 眉、hungry drool、lonely tear、dazed 晕圈+错位眼、confused 问号、startled 放大眼+张嘴、happy 笑脸、excited 笑脸+星光、focused 眯眼+眉、curious 星光+单眼上移，0 横向溢出 | 修改 src/components/PetCompanion.tsx、PetCompanion.css；新增白名单 fallback 与通用 mood marks，未知 mood 回 idle，不改后端 DTO、轮询、状态机或事件流；下一步 B2-P3 六帧跑动 + 面向方向
B2-P3 六步跑动 + 面向方向 | 完成 | 依赖：B2-P2 | 验证：npx tsc --noEmit + npm test 6 files/20 tests + npm run lint + npm run build 3483 modules + git diff --check 全绿；浏览器硬证据：right `matrix(0.88,0,0,0.88,0,0)` / left `matrix(-0.88,0,0,0.88,0,0)`，pet-run-body 与 pet-limb-a 实际运行并发生帧变化，0 横向溢出，0 console error | 修改 src/components/PetCompanion.tsx、PetCompanion.css；新增 PetDirection，漫游目标与水平拖拽更新左右朝向；720ms/6-step 驱动身体、阴影、尾巴、耳/触角、臂腿与光翼，其他行为动画通过 --pet-facing 保留朝向；下一步 B2-P4 事件到 visual pose 映射
B2-P4 Rust 事件到 visual pose 映射 | 完成 | 依赖：B2-P3 + Rust PetView 真值 | 验证：tsc + npm test 6/20 + lint + 全量前端守卫 + build 3484 + diff check 绿；浏览器生产 DOM 暴露 stage/machine/mood/pose/direction，36×32 viewBox、pet-breathe 生效、0 横向溢出 | 新增 src/components/petVisualPose.ts；PetCompanion 消费 machine/recent_events/crafting 与既有行为，映射 Code/Read/Execute/Network/AgentSpawn/ToolCancelled/Timeout/Refused/Maxed/Connected/Crashed/Feed/Play/Poke 到稳定 pose 和离散动画
B2-P5 v2 contracts normalize + MOCK_PET 补齐 | 完成 | 依赖：B2-P4 | 验证：test-pet-contracts + tsc + npm test 6/20 + lint + 全量前端守卫 + build 3484 + diff check 绿 | 扩展 petContracts.ts 的 needs/HSM/day_part/recent_events/crafting/cosmetics/achievements/完整 stats；MOCK_PET 同步；petPersistence 排除派生 view 字段，保持 restore 载荷边界；下一步 B2-P6 cosmetics overlay + equip/unequip
B2-P6 11 件 cosmetics overlay + equip/unequip | 完成 | 依赖：B2-P5 + 已认可 B2-02 Preview | 验证：tsc + npm test 6/20 + lint + 全量前端守卫 + build 3484 + diff check 绿；浏览器装备夜光贝雷帽后 data-cosmetic=beret、2 overlay 节点、0 横向溢出 | PetCompanion 生产 SVG 接入 11 个稳定 id overlay，衣橱按钮调用既有 pet_action equip/unequip；未改 src-tauri
B2-P7 衣橱、needs、crafting、day_part UI | 完成 | 依赖：B2-P6 | 验证：浏览器按需面板 11 件目录、needs 80/70/100、feed/play 可触发 mock pose、默认透明 shell 守卫更新后全绿 | 新增非长期常驻的小型状态/衣橱面板，消费 hunger/fun/loneliness/bond/crafting/day_part/cosmetics；成就面板未制作；下一步 B2-P8 真实 Tauri 验收
```
