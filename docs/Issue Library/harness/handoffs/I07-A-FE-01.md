# I07-A-FE-01 handoff

## 状态

- owner: A (`codex-role-a`)
- branch: `a/I07-A-FE-01-file-source-clear`
- base: `origin/main` at `723b6c9` (I04-A-BE-01 merged)
- current: implementation complete; review pending

## 已完成

- FileSheet 会话区新增“清除选择”，将 `targetSource` 显式设为 `null`。
- source 清空时，FileSheet 父层清理 active diff、编辑器内容、selection、instruction 和 truncated 瞬态。
- GitPanel 在 source=null 时清空 staged/unstaged/history/error/expanded commit；ViewsPanel 清空 entries/error。
- 两个 panel 保留 source/disposed effect guard，source 切换时旧请求不能回写。
- 新增 FileSheet source reset Vitest，并更新 file-sheet/Git legacy guards。

## 验证

- `npm run test -- --run src/sheets/file/__tests__/fileSheetSourceReset.test.ts`: 1 passed。
- `npm run test`: 43 files / 209 passed。
- `npm run test:legacy`: 全部通过。
- `npm run lint`: 通过。
- `npm run build`: 通过，3505 modules transformed。
- Harness validator: 45 cards，DAG 无环。
- `git diff --check`: 通过（仅 CRLF 转换提示）。

## 注意事项

- open tabs metadata 保留既有持久化策略；source=null 时主编辑器由 FileTabView 显示“未指向会话”，不改变 #6 文件视图策略。
- 工作树中保留用户既有 README 修改与截图删除，未纳入提交。
