# Handoff: I01-A-BE-01

- 角色/模式：A / `longrun-a`
- 远端分支：`a/I01-A-BE-01-agent-context`
- Base commit：`572fcd6`
- HEAD commit：`b5def0f`
- 状态：`implemented`，L1 已通过，等待 review/integration

## 已完成

- 新增结构化 `AgentContextKey { agent_id, source }`，同名 source 按 Agent 维度保持不同 identity。
- 为 source-only GUI 兼容路径增加 `active_context_for_source` 与 `runtime_for_context`，显式校验 context Agent 等于当前 active Agent。
- workspace root 读取同时校验 context、runtime 实例和 source session 映射。
- 删除 Git workspace root 在 source 缺失时回退 `agent_cwd` 的行为；现在统一返回 `git_error`，内部保留 `session_not_found` 原因。
- 新增未知 source 和跨 Agent 同名 source 两组回归测试。

## 实际验证

| 命令/行为 | 结果 | 证据等级 | 证据路径 |
|---|---|---|---|
| RED：`cargo test --lib git_workspace_root_rejects_missing_source_in_active_runtime` | 修复前失败，实际返回 fallback cwd | L1 | `workspace_cmds.rs` test |
| `cargo test --lib workspace_cmds::tests` | 通过；3/3 | L1 | 命令输出 |
| `cargo test --lib runtime::tests::agent_context_key_keeps_agent_dimension` | 通过；1/1 | L1 | 命令输出 |
| `cargo test --lib --no-run` | 通过 | L1 | 命令输出 |
| `cargo test --lib` | 通过；425 passed，4 ignored，0 failed | L1 | 命令输出 |
| `cargo build --release` | 通过；GNU toolchain + MinGW linker | L1 | 命令输出 |
| `npm run lint` | 通过 | L1 | 命令输出 |
| `npm run build` | 通过；3504 modules transformed | L1 | 命令输出 |
| `git diff --check` | 通过 | L1 | 命令输出 |

## 工作区

```text
任务分支基于 origin/main 的 Harness 全量入库基线。
任务实现只修改 runtime.rs、session/mod.rs、workspace_cmds.rs；README 修改与截图删除属于领取前/并行用户工作，未纳入任务提交。
```

## 阻塞与失败证据

- `cargo fmt -- --check` 失败于任务范围外的既有格式漂移（首先出现在 `acp/mod.rs`，并覆盖其他未改文件）；未执行全仓格式化，避免扩大 scope。
- 第一次 `cargo build --release` 在 180 秒工具超时后已生成二进制但无完成状态；确认无残留 cargo/rustc 进程后增大超时重试，第二次 2 分 03 秒成功。
- 工作期间 `origin/main` 新增 Harness 全量入库提交；已将任务分支 fast-forward 到 `572fcd6` 后重新落入实现提交，未覆盖用户改动。

## 下一条确定动作

1. 推送分支并发起 review；CI 通过后由 A 集成。
2. 合并后按依赖领取 `I01-A-FE-01`，实现聚焦非 active Agent Sheet 的切换事务。

## 不得假定

- 本卡只建立 Release 1.x 单活兼容 guard；并未把全部 Session/Chat IPC 改成显式 agentId。
- L1 通过不代表真实 Peri/Hermes 同名 Session 的 L3 已通过。
- 任何后续 workspace/Git wrapper 都不得重新引入 source 缺失时的 active Agent cwd fallback。
