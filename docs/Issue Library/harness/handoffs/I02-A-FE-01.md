# Handoff: I02-A-FE-01

- 角色/模式：A / `longrun-a`
- 远端分支：`a/I02-A-FE-01-lifecycle-capabilities`
- Base commit：`c0eddb9`
- HEAD commit：`6ce048f`
- 状态：`review_pending`；L1 已通过；PR：待创建

## 已完成

- `resolveCapabilitySnapshot` 的 `connected` 改为唯一依据 `AgentStatus.status === 'connected'`。
- 新增 `capabilitiesKnown`，只表示 capabilities 是否为已识别对象；能力缺失不再反推断线。
- connecting/reconnecting/error/crashed/disconnected 等非 connected 代际即使携带旧 capabilities，也统一 `connected: false`。
- connected 且 capabilities 为 null/缺失时仍允许基础文本链路；图片能力保持保守关闭，文本附件过滤继续可用。
- 更新既有 agent-contracts legacy 守卫，补充生命周期 × capabilities 矩阵 Vitest。
- 未修改 scope 外的 InputBar/AttachWidget；它们继续通过 `useAgentCapabilities()` 消费统一快照，因此自动获得新的 lifecycle connected 语义。

## 实际验证

| 命令/行为 | 结果 | 证据等级 | 证据路径 |
|---|---|---|---|
| RED：capability selector matrix | 修复前 connected + null 被判 disconnected，reconnecting + 旧 capabilities 被判 connected | L1 | `src/infrastructure/acp/__tests__/agentContracts.test.ts` |
| GREEN：focused Vitest | 通过；2 passed | L1 | 命令输出 |
| `npm run test` | 通过；39 files，199 passed | L1 | 命令输出 |
| `npm run test:legacy` | 通过；legacy 前端守卫全套通过 | L1 | 命令输出 |
| `node --experimental-strip-types scripts/test-agent-contracts.mts` | 通过 | L1 | 命令输出 |
| `npm run lint` | 通过 | L1 | 命令输出 |
| `npm run build` | 通过；3505 modules transformed | L1 | 命令输出 |
| `python docs/Issue Library/harness/scripts/validate_harness.py` | 通过；45 张任务卡，依赖 DAG 无环 | L1 | 命令输出 |
| `git diff --check` | 通过 | L1 | 命令输出 |

## 工作区

```text
任务实现只修改 src/infrastructure/acp/agentContracts.ts、scripts/test-agent-contracts.mts、对应 Vitest 与本卡 handoff/metadata。
领取前已有的 docs/Issue Library/harness/README.md 修改和 docs/美化截图/ 下截图删除未纳入任务提交，继续保留。
```

## 阻塞与失败证据

- 首次 legacy 运行因旧脚本仍把 `capabilities:null` 当断线而失败；已把脚本矩阵改为显式区分 lifecycle disconnected 与 capabilities 未协商，重跑通过。
- 本卡只要求 L1；未执行网页 L2 或真实 ACP L3。

## 下一条确定动作

1. 提交并推送本分支，创建 PR 进入 review。
2. 合并后可由共享角色 S 领取 `I02-A-TEST-01`；角色 A 按 INDEX 继续领取后续 ready FE 卡。

## 不得假定

- `capabilitiesKnown` 不等于任何单项能力可用；扩展 gate 仍必须读取对应能力字段。
- L1 通过只证明受控前端契约正确，不证明真实 Agent initialize 或 provider 能力协商结果。
