# I04-A-FE-01 handoff

## 状态

- owner: A (`codex-role-a`)
- branch: `a/I04-A-FE-01-reconnect-command-state`
- base: `origin/main` at `f38142a` (I03-A-FE-01 merged)
- current: implementation complete; review pending

## 已完成

- Settings 的 `reconnectPending` 与 `reconnectCommandError` 改为独立 UI command state，不再用 `beginReconnect`/`failReconnect` 写 `agentStatuses`。
- 新增 `runReconnectCommand`：command resolve 不伪造 lifecycle；command reject 后查询 `agent_status` 并应用权威快照。
- 若 `agent_status` 对账也失败，保留最后权威 snapshot，只通过 runtime error 与独立操作错误报告失败。
- Settings 单独显示“重连操作失败”，状态标签继续来自统一 lifecycle selector。
- 更新 reconnect 两份 legacy guard，并新增行为级 Vitest，覆盖 accepted/rejected/reconciliation-failed 三条路径。

## 验证

- RED：focused Vitest 因 `reconnectCommand` 模块缺失失败。
- GREEN：focused Vitest 3 passed。
- `npm run test`: 41 files / 205 passed。
- `npm run test:legacy`: 全部通过。
- `npm run lint`: 通过。
- `npm run build`: 通过，3505 modules transformed。
- Harness validator: 45 cards，DAG 无环。
- `git diff --check`: 通过（仅 CRLF 转换提示）。

## 注意事项

- generation 单调接收属于下一张 `I04-A-BE-01`，本卡未提前修改 runtimeStore/lifecycle。
- 工作树中保留用户既有 README 修改与截图删除，未纳入提交。
