# I03-A-FE-01 handoff

## 状态

- owner: A (`codex-role-a`)
- branch: `a/I03-A-FE-01-unknown-status`
- base: `origin/main` at `2b623f3` (I02-A-FE-01 merged)
- current: implementation complete locally; review pending

## 已完成

- `AgentConnectionStatus` 增加 `unknown`；缺少 lifecycle status 且非 crashed 时归 `unknown`，显式非法状态仍归 `error`。
- 新增 `selectAgentStatus(agentId, activeAgent, statuses)`：active 无快照返回 `unknown`，非 active 返回 `inactive`，已有快照保留后端状态。
- Settings、WorkspaceTitlebar、SheetTabStrip 均改用 selector，移除 Settings 的乐观 `connected` fallback。
- 补充 Vitest 状态矩阵与 unknown 回归脚本；同步更新 titlebar 结构守卫以验证 selector 接线。
- 未修改后端 lifecycle；未修改 I02 能力 selector 及其范围外 consumer。

## 验证

- RED：`npm run test -- --run src/components/settings/__tests__/agentStatusSelector.test.ts` 失败（selector 缺失、缺失状态错误为 connected）。
- GREEN：同命令 3 passed。
- `npm run test`: 40 files / 202 passed。
- `npm run test:legacy`: 全部通过。
- `npm run lint`: 通过。
- `npm run build`: 通过，3505 modules transformed。
- `python docs/Issue Library/harness/scripts/validate_harness.py`: 45 cards，DAG 无环。
- `git diff --check`: 通过（仅 CRLF 转换提示）。

## 注意事项

- 工作树中保留用户既有的 `docs/Issue Library/harness/README.md` 修改及 `docs/美化截图/` PNG 删除，未纳入本任务提交。
- `src/infrastructure/acp/useAgentCapabilities.ts` 位于本卡 scope 外；I02 已保证能力缺失不反推连接状态，本卡未越界修改。
