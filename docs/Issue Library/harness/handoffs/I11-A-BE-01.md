# I11-A-BE-01 handoff

## 状态

- 角色/模式: 天玑-全栈实现 / longrun-a（Reply 模式，未改 issue 状态）
- 远端分支: `a/I11-A-BE-01-webview-new-window`（已 push origin，2026-08-10）
- base: `origin/main` @ `990b456`；HEAD: `7930996`（1 commit）
- 状态: implementation complete; review pending（交玉衡审查；未合并、未建 PR）

## 已完成

- **新窗口 API contract 确认（以锁定依赖源码为准，非经验）**：
  - `WebviewBuilder::on_new_window<F: Fn(Url, NewWindowFeatures) -> NewWindowResponse<R> + Send + 'static>`，tauri 2.11.5 `src/webview/mod.rs:585-591`。
  - `NewWindowResponse<R: Runtime> { Allow, Create { window }, Deny }`，tauri 2.11.5 `src/webview/mod.rs:239-255`；`Deny` 取消窗口创建。
  - `NewWindowFeatures` 由 `tauri::webview` re-export（`mod.rs:21`）。
  - `tauri::Url` = `url::Url`（tauri `lib.rs:83`），Cargo.lock 仅 url 2.5.8 单一版本，与项目直接依赖同型。
  - `WebviewBuilder` 为 `unstable_struct!`（feature `unstable` 或 `test` 下 pub）；项目 `src-tauri/Cargo.toml` 已启用 `unstable`。
  - Windows 取消语义：tauri-runtime-wry 2.11.4 `lib.rs:4908-4962` 将响应映射给 wry；`Deny` → wry 0.55.1 `webview2/mod.rs:702-786` 的 `NewWindowRequested` 处理器 `args.SetHandled(true)` 取消创建。
  - 源码证据路径（本机锁定）：`F:\Coding\rust\.cargo\registry\src\index.crates.io-1949cf8c6b5b557f\tauri-2.11.5\src\webview\mod.rs`。
- **共享白名单落地**：新增 `is_allowed_browser_url(&url::Url) -> bool`（`src-tauri/src/browser.rs:60-66`）：http/https 放行；about 仅允许初始化约定 `about:blank`（引用 `BROWSER_INITIAL_URL` 常量）；file/javascript/data/devtools/chrome 一律拒绝。
- **三处共用同一白名单（消除重复 scheme 检查）**：`navigate()`（`browser.rs:228-230`）、`on_navigation`（`browser.rs:150`）、`on_new_window` 回调（`browser.rs:151-157`）。
- **on_new_window 回调落地（ISSUE-11 方案 A）**：一律返回 `Deny` 取消原窗口创建；白名单内 URL 复用当前 pylon-browser WebView 导航（调用既有 `navigate()`）；非白名单 URL 仅取消、不导航。回调内 URL 直接以 `is_allowed_browser_url(&url)` 判定，无第三份 scheme 检查。
- **TDD**：先写白名单 scheme 矩阵测试 → 编译失败（RED）→ 实现 → 440 测试全绿（GREEN）。

## 实际验证（2026-08-10，全在新分支 HEAD 上执行）

| 命令 | 结果 | 证据等级 | 证据路径 |
|------|------|----------|----------|
| `cd src-tauri && CARGO_TARGET_DIR=/f/pylon-target cargo test --lib` | 440 passed, 0 failed, 4 ignored；浏览器模块 8/8 含新增 `url_whitelist_allows_http_https_and_init_about_blank` | L1 | 本回合终端输出 |
| `cargo test --lib --no-run`（focused） | 通过（编译验证，已含于上条） | L1 | 同上 |
| `npm run lint` | exit 0，`eslint src/` 无告警 | L1 | 同上 |
| `npm run build` | exit 0；`tsc -b && vite build`，3522 modules transformed，dist/ 正常产出 | L1 | 同上 |
| `git diff --check` | 通过 | L1 | 同上 |

## 工作区 git status

- 提交：`7930996 feat(browser): [I11-A-BE-01] 统一 scheme 白名单并落地新窗口回调`（1 file, +47/-6，仅 `src-tauri/src/browser.rs`）。
- 本卡改动仅落在 scope.allow 的 `src-tauri/src/browser.rs`；`dist/`、`node_modules/` 均 gitignored，未入提交。

## 阻塞与失败证据（环境层面，不影响本卡结论）

- C: 磁盘近乎耗尽（构建前 581M 空闲，npm install 后 13M）：cargo target 重定向至 F:（`CARGO_TARGET_DIR=/f/pylon-target`）；node_modules 安装在 C:（junction 方案被 npm 11 移除，回退真实目录）。
- `frontendDist` 指向的 `../dist` 缺失导致 `generate_context!` 编译 panic（前置环境问题，与本卡改动无关）：以 gitignored 空 `dist/` 目录满足宏存在性检查，`npm run build` 已生成真实产物。
- esbuild postinstall 被 npm 11 allowScripts 拦截：平台二进制随 `@esbuild/win32-x64` 包已就位，vite build 不受影响。
- 首次 `npm install` 前 node_modules 不存在（`npm install` 补齐，413 packages）。

## 下一条确定动作

- 交玉衡审查 I11-A-BE-01（本 handoff + commit `7930996`）。
- 真实 WebView2 新窗口行为（window.open/百度 target 跳转、恶意 scheme 拒绝、导航复用）属 L3，归 I11-A-BE-02；本卡只交付 L1 的源码 contract 证据与白名单单元测试。

## 不得假定

- `on_new_window` 在真实 WebView2 上的运行时行为未经真机验证（L1 到源码 contract + 编译/单元测试为止）；回调与白名单的绑定正确性由编译与 440 项测试保证，事件触发路径需 I11-A-BE-02 L3 验证。
- `navigate()` 现按共享白名单放行 `about:blank`（初始化约定），行为较原先纯 http/https 有收窄/放行调整，均已在文档与测试中固化。
