# Handoff: I13-A-FE-02

- 角色/模式：A / `longrun-a`
- 远端分支：`agent/agent/32a9b4ca`
- Base commit：`93b23a4`（origin/main；2026-08-10 rebase 至当时最新 main，I04-A-TEST-01 合入后）
- HEAD commit：`7ae7b21`（rebase 后）
- 状态：实现完成，L1 全绿，待玉衡审查 / 天权合并

## 已完成

- 新增 `src-tauri/src/session/retention.rs`（Rust 实施契约，不含删除逻辑）：
  - **D-03 三档模式**：`RetentionMode::{Permanent, ByTime, ByCount}`（serde snake_case）。
  - **档位与默认值（实施契约，前后端一致）**：`TIME_DAYS_TIERS=[7,30,90,180,365]`（默认 30）、`COUNT_LIMIT_TIERS=[100,500,1000,5000,10000]`（默认 1000）。
  - **D-15 回退语义**：`default_policy()` → Permanent；`parse(json)` 对解析失败、未知模式、越档值、缺字段一律回退永久保存；`is_valid()` 校验 days/count 必须落在档位内。
  - 契约测试 12 个（模式解析、档位与契约一致、默认值、回退矩阵、serde roundtrip）。
- `src-tauri/src/session/mod.rs` 注册 `mod retention;`（不重导出，避免命名空间污染；与 msg_repo 同模式）。
- 新增 `src/components/settings/historyRetentionPolicy.ts`（前端设置契约）：
  - 常量与 Rust 侧逐字一致（档位/默认值/模式枚举/`RETENTION_STORAGE_KEY='pylon-history-retention'`）。
  - `readRetentionPolicy`（try/catch JSON 解析 + 严格校验，非法回退永久保存）、`writeRetentionPolicy`（静默失败）、`isRetentionPolicyValid`、`retentionPolicyImpact`（非永久策略返回 `kind:'warn'` 影响文案，明确「保存策略不会立即删除任何消息，自动清理由后端在事务边界安全执行」；永久返回 null）。
  - 契约测试 17 个（默认值、回退矩阵、合法读取、写读往返、影响文案、档位常量、模式选项、校验函数）。
- 新增 `src/components/settings/HistoryRetention.tsx`（UI 组件，挂载于设置「全局」页，窗口/配置备份同区）：
  - 模式下拉 + 档位下拉（按时间天数 / 每会话条数）+ 影响提示（D-15 必须显示预计影响）+ 基线说明「设置页只保存策略，不会直接删除消息」。
  - 组件测试 7 个（默认永久无提示、by_time/by_count 档位与影响提示、切回永久隐藏、只写策略 key 无删除副作用、档位切换更新提示并持久化）。
- `src/components/Settings.tsx` 引入并渲染 `<HistoryRetention />`；`src/components/Settings.css` 增加 `.set-group-inner-title` / `.set-impact`。
- 契约落档：`docs/Issue Library/ISSUE-13.md` 新增「I13-A-FE-02 实施契约（2026-08-10 落地，验收地址）」小节与施工日志行。

## 实际验证

| 命令/行为 | 结果 | 证据等级 | 证据路径 |
|---|---|---|---|
| `cargo test --lib retention`（红：todo! 桩） | 12 failed：`not yet implemented` | L1 | 命令输出 |
| `cargo test --lib retention`（绿：实现后） | 12 passed | L1 | 命令输出 |
| `cargo test --lib`（全量回归） | 471 passed；0 failed；4 ignored | L1 | 命令输出 |
| `npm run test`（commands.focused） | 346 passed（56 files）；0 failed | L1 | 命令输出 |
| `npm run lint`（commands.broader） | 通过 | L1 | 命令输出 |
| `npm run build`（commands.broader） | 通过；built in 4.89s | L1 | 命令输出 |
| `git diff --check`（commands.broader） | 通过 | L1 | 命令输出 |

- 「不绕过 Rust 删除」（AC-1）：前端仅持久化策略（唯一副作用 key 为 `pylon-history-retention`，组件测试断言）；全仓库无新增任何删除 command / 删除路径；实际删除仍由 Rust 消息仓库（`msg_repo`）在事务边界执行。手动「立即清理」按契约**未实施**（后端清理调度 command 未实现）。

## 工作区

- 分支 `agent/agent/32a9b4ca`，实现 4 commit 领先 base `93b23a4`（origin/main）：`174f5e2`（Rust 契约）→ `81464cb`（前端契约）→ `b8185db`（UI + 挂载 + 契约模块更名）→ `7ae7b21`（ISSUE-13 落档）。
- 2026-08-10 rebase 至 origin/main `93b23a4`（I04-A-TEST-01 合入后；该 merge 仅新增 handoff 与 reconnect 测试，与本次改动无文件相交，rebase 无冲突）。
- 构建缓存统一重定向 `CARGO_TARGET_DIR=/d/pylon-target/I13-A-FE-02`（F: 盘满）。
- `dist/index.html` 桩文件与 npm install 产生的 package.json/package-lock 变更均为环境设置噪声，**未**纳入本任务提交（dist 属 PROTECTED 路径）。

## 阻塞与失败证据

- 无产品或契约阻塞；I06-A-DATA-01 已合 main，无依赖缺口。
- 环境阻塞（已解决）：
  - Windows 大小写不敏感文件系统模块解析冲突：`import '../HistoryRetention'` 曾解析到同目录 `historyRetention.ts`（.ts 优先于 .tsx）导致空模块命名空间「Element type is invalid」。修复：契约模块更名 `historyRetentionPolicy.ts`（含测试），根因是扩展名歧义而非导入路径错误。
  - F: 盘 100% 满 → `CARGO_TARGET_DIR` 重定向 D:；MSYS `/usr/bin/dlltool` 遮蔽 mingw → PATH 前置 mingw64；RUSTUP_HOME/CARGO_HOME 未注入 → 显式导出。
- scope 偏差（预期，请审查裁决）：`docs/Issue Library/harness/handoffs/I13-A-FE-02.md` 不在 scope.allow 列内，系 evidence.artifacts 显式要求（I05-A-FE-01 / I06-A-DATA-01 同先例）。代码改动全部落在 allow 列内。

## 下一条确定动作

1. 玉衡审查本分支；如通过，天权合并到 main。
2. 合并后解锁 `I13-A-FE-03`（Gateway 备份风险提示，依赖 I12-A-SEC-01）与后续「立即清理」后端清理卡（须消费本契约：Rust `retention.rs`、前端 `historyRetentionPolicy.ts`，并满足 D-15 独立显式操作 + 预计删除范围 + 确认）。

## 不得假定

- L1 测试通过不代表真实 Tauri 应用 L2/L3 已通过；`HistoryRetention` 当前挂载于设置「全局」页，I13-A-FE-01 落地后按 ISSUE-13 契约迁入「工作区」域。
- 本卡未实现任何删除逻辑与「立即清理」入口；后续后端清理卡不得把删除命令伪装成设置页默认行为。
- `validate_harness.py` 可能对 handoff 路径标记 scope 违规，为上文记录的预期偏差。
