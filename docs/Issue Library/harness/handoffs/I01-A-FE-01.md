# Handoff: I01-A-FE-01

- 角色/模式：A / `longrun-a`
- 远端分支：`a/I01-A-FE-01-sheet-focus`
- Base commit：`10283a1`
- HEAD commit：`ceb6cfb`
- 状态：`review_pending`；L2 已通过；PR：待创建

## 已完成

- 新增 `activateAgentSheet` 事务边界：Release 1.x GUI 只有一个 active Agent，任何目标 Agent Sheet 在获得 focus/open 之前先执行 `switch_agent`，成功后才允许进入目标 Sheet。
- `SheetTabStrip` 的鼠标点击和左右键盘导航共用该事务；切换 pending 时目标 tab disabled/标记 busy，失败不调用 `onFocus`。
- `SheetLauncher` 的最近 Agent Sheet 和 Agent 新建入口同样经过事务，消除了“Launcher 直接打开非 active Agent Sheet”的旁路。
- 切换事务复用既有 `switchAgentTransaction`，继续保持 `switch → reset runtime → set active → dispatch → focus/open` 的顺序和失败不改变 active Agent 的契约。
- 新增 tab 与 launcher 组件回归测试，覆盖成功、失败、active 直达、键盘导航和 pending 禁用行为。

## 实际验证

| 命令/行为 | 结果 | 证据等级 | 证据路径 |
|---|---|---|---|
| RED：Sheet tab focused test | 修复前非 active tab 直接 focus，且没有 `switch_agent` 调用 | L1 | `sheetTabStripAgentSwitch.test.tsx` |
| GREEN：Sheet tab focused test | 通过；4 passed | L1 | 命令输出 |
| RED：Sheet launcher focused test | 修复前 Launcher 直接 `onOpenSheet`，没有切换事务 | L1 | `sheetLauncherAgentSwitch.test.tsx` |
| GREEN：Sheet launcher focused test | 通过；1 passed | L1 | 命令输出 |
| `npm run test` | 通过；38 files，197 passed | L1 | 命令输出 |
| `npm run lint` | 通过 | L1 | 命令输出 |
| `npm run build` | 通过；3505 modules transformed | L1 | 命令输出 |
| `python docs/Issue Library/harness/scripts/validate_harness.py` | 通过；45 张任务卡，依赖 DAG 无环 | L1 | 命令输出 |
| `git diff --check` | 通过 | L1 | 命令输出 |
| 本地网页：Launcher 选择 Hermes | 先切换后进入 Hermes Sheet；随后点击非 active Peri tab，先切换后选中 Peri | L2 | `localhost:5173/` 浏览器 DOM 快照 |

## 工作区

```text
任务实现只修改 src/workspace-sheets/** 与本卡 handoff/metadata。
领取前已有的 docs/Issue Library/harness/README.md 修改和 docs/美化截图/ 下截图删除未纳入任务提交，继续保留。
```

## 阻塞与失败证据

- 初次 L2 页面验收发现 SheetLauncher 是未覆盖的直接聚焦旁路；已补充 Launcher 事务和回归测试后重新完成 L2。
- 未执行真实 Tauri/ACP L3；本卡要求等级为 L2，真实 runtime/ACP 证据由后续 `I01-A-TEST-01` 负责。

## 下一条确定动作

1. 提交并推送本分支，创建 PR 进入 review。
2. CI 通过后由角色 A 合并，随后按依赖领取 `I01-A-TEST-01`。

## 不得假定

- 本卡只保证 Release 1.x 单活 GUI 的 Sheet 聚焦入口；不宣称多个 Agent runtime 并行发送。
- `switch_agent` 的真实连接/ACP 结果仍需 L3 现场证据；网页模式使用 mock/fallback，只验证 UI 事务顺序和禁用边界。
