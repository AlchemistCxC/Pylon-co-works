# I04-A-BE-01 handoff

## 状态

- owner: A (`codex-role-a`)
- branch: `a/I04-A-BE-01-generation-guard`
- base: `origin/main` at `413fb5b` (I04-A-FE-01 merged)
- current: implementation complete; review pending

## 已完成

- `runtimeStore.setAgentStatus` 统一经过 `shouldAcceptAgentStatus`，低 generation 事件不能覆盖更高 generation snapshot。
- 同 generation 仍按事件到达顺序更新；缺失 generation 因无法比较而保留接收语义。
- App bootstrap 快照与 `pylon:agent-status` listener 共用该写入口，因此事件/快照语义一致。
- 后端核对确认 `replace_agent_client` 在成功激活时以原子 `fetch_add` 递增 `client_generation`，`emit_agent_status` 与 `agent_status` payload 均携带当前 generation；本卡未改后端 wire。
- 补充 generation matrix Vitest，并把 reconnect/listener legacy guards 更新为新 guard contract。

## 验证

- RED：`agentStatusGeneration.test.ts` 低代际用例失败（旧事件覆盖新 snapshot）。
- GREEN：focused matrix 3 passed。
- `cargo test --lib --no-run --offline`: 通过。
- `cargo test --lib --offline`: 425 passed，4 ignored。
- `npm run test`: 42 files / 208 passed。
- `npm run test:legacy`: 全部通过。
- `npm run lint`: 通过。
- `npm run build`: 通过，3505 modules transformed。
- `cargo build --release --offline`: 通过。
- Harness validator: 45 cards，DAG 无环。
- `git diff --check`: 通过（仅 CRLF 转换提示）。

## 注意事项

- 后端 generation 生产规则已存在且通过既有 Rust 测试；本卡只收口前端迟到事件接收，未改变 `src-tauri`。
- 工作树中保留用户既有 README 修改与截图删除，未纳入提交。
