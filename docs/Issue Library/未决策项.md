# 未决策项

> 所有需要用户拍板的产品决策已经完成，并已写入对应 Issue 的“已拍板决策”章节。
>
> 本文件现在只保留：已经有产品方向，但尚未形成完整代码级实施契约的事项。长行程 Agent 可以在源码与测试允许的范围内细化这些方案；如果发现会改变产品行为，必须重新标记为产品阻塞，不得自行猜测。

## 一、产品决策未完成

当前没有已确认必须由用户拍板、且能在不补充真实证据的情况下直接选择的产品决策。

以下事项在实施前需要先取得证据或形成提案；若提案引入产品行为取舍，再回到本节由用户拍板：


## 二、已有决策但实施契约仍不完整

## 三、暂不需要产品拍板的实现参数

以下事项可以由施工 Agent 按源码、依赖和测试确定，不视为产品未决策：

- SQLite 数据库放置于 Pylon 用户数据目录；
- SQLite 启用 WAL、`foreign_keys=ON`、`busy_timeout`、事务写入和 schema version；
- 消息分页默认每页 100 条，使用 `beforeSeq` 游标，不使用长历史 `OFFSET`；
- `messageId` 采用 UUIDv7、ULID 或其他满足全局唯一要求的实现，具体取决于 Rust 依赖；
- Diff 自动模式的宽度阈值由真实 FileSheet 可用宽度和验收确定；
- idle timer 精度、并发上限检查周期和后台导入调度间隔。

## 长行程 Agent 执行规则

1. 当前没有待用户拍板的产品决策，Harness 不应再因“产品未决策”阻塞现有任务卡。
2. 第二节事项属于实施契约细化，不得改变已拍板产品行为。
3. 如果细化过程出现新的产品取舍，应在本文件新增“产品决策未完成”条目并阻塞关联 task。
4. 实施方案形成后，将措施写入关联 Issue 的已拍板决策/施工方案与施工日志，并从本文件移除对应条目。

## 四、Harness v2 阻塞映射（2026-08-09）

本轮产品取舍已完成，以下条目不再等待用户拍板，转为实现任务：

| 条目 | 关联 Task | 状态 | 说明 |
|---|---|---|---|
| Gateway AES-256-GCM 凭据文件 | `I12-A-SEC-01` | `ready` | 按 ISSUE-12 D-08 实施并做真实 ACL/损坏恢复验证 |
| ACP chunk 内存诊断环形缓冲 | `I06-A-DATA-01` | `ready` | 按 ISSUE-06 D-16 实施容量、TTL、删除和脱敏导出 |
| SQLite 未完成 attempt 恢复 | `I06-A-DATA-01` | `ready` | 按 ISSUE-06 D-17 实施启动前扫描、interrupted 与 retryOf |
| replay 复读/串会话 | `I06-A-FE-03` | `blocked_evidence` | 仍需真实失败完整时间线；禁止继续猜测式去重补丁 |

只有最后一项仍是证据阻塞，不是产品决策阻塞。
