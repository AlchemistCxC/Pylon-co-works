# Pylon 后端手册（源码驱动）

> 仓库：`G:\Project\prism-desktop` · 后端：`src-tauri/`（lib.rs ~3,800 行 + acp.rs ~1,570 行 + 9 小文件 + pet-core + gateway/）
> 真值：Pylon 源码 > Peri `F:\A-I\Agent\Peri` > Prism `G:\Project\prism` > 官方 schema（agent-client-protocol-schema 1.4）
> Hermes 参考（B10 gateway 设计）：`F:\Hermes\hermes-agent\gateway\`（platforms/base.py、platforms/qqbot/adapter.py、dead_targets.py）
> 工具链：Rust GNU `F:\Coding\rust\toolchain\.cargo\bin` + windres `F:\Coding\c\mingw64\mingw64\bin`；`unset RUSTFLAGS`；`TMP=F:/tmp`；cargo `--offline`
> 本文是源码的**索引与契约**，不是源码替代品。行号、状态、commit 都可能过期——以 `git status` 和当前源码为准。
> 重构日期：2026-07-31（对齐 main@d7c0ade）

---

## 1. 状态快照

### 1.1 完成度摘要

| 面 | 状态 | 证据 |
|:--|:--|:--|
| 对话核心闭环 | ✅ 完成 | initialize→new→prompt→流式 update→cancel→close→load，官方 schema 类型化参数 |
| 崩溃自动重连 | ✅ 完成 | 指数退避 5 次（2s→32s）、防重入、keep_sessions + generation 迁移；mock window 集成测试 |
| Session Inspector | ✅ v1 | active runtime 聚合：agent + summary + sessions 明细 |
| 会话管理 | ✅ 完成 | 映射/持久化/导出/取消/超时 |
| 宠物 | ✅ 完成 | pet-core 独立 crate：5 阶段/7 情绪/记忆/睡眠/落盘持久化 |
| Hermes 兼容 | ✅ wire 实测 | `scripts/hermes-wire-test.py` 回归；差异适配表见 §3.3 |
| MCP 配置 | ✅ 校验+持久化完成 | 校验/序列化 + 落盘（pylon-mcp.json 原子写，重启恢复，B4.2） |
| Prism 网关 | ✅ route audit + 错误 DTO 统一 | 39 命令逐一对照 Prism route（B3）：无伪能力；全部改 PylonError::Prism |
| 权限审批流 | ❌ 未实现 | B9（P0，已排期） |
| Git/多 Agent/PTY | ❌ 未实现 | B5/B7/B8 |

### 施工进度（2026-07-31 交接时点）

| 项 | 状态 | 证据 |
|:--|:--|:--|
| B7a-1 AgentRuntime/Manager 结构 | ✅ 已提交 | `runtime.rs`（per-agent 隔离运行时 + 2 单测），9d29d3b |
| B7a-2 AppState 迁移 + 命令层接线 | ✅ 已提交 | lib.rs 8 个 per-agent 字段 → runtimes manager（990e9b4 + 前置 16fecb2）；dead_code warning 清零；自动重连 per-runtime 化（active_agent 复查防顶回）；runtime 新增 session_creation/get_or_create |
| BE-B10-001 route（EntityBinding 解析） | ✅ 已提交 | `gateway/route.rs`，8 测试，a9b300c |
| BE-B10-002 truncate（长回复切断） | ✅ 已提交 | `gateway/truncate.rs`，8 测试，30e3cc5（主控修 1 处测试断言） |
| BE-B10-003 dedup（重放去重） | ✅ 已提交 | `gateway/qq/dedup.rs`，5 测试，293a6a8 |
| BE-B10-004/005/006（QQ auth/events/send 移植） | ✅ 已提交 | auth 060a79b（QqAuth+get_gateway_url）；types+events 041060a（事件类型全集 + 附件分类/@剥离/时间戳/引用 8 单测）；send 99ecc14（send_message/upload/send_media/typing 5 单测，本地 TCP 桩；rand 0.10 新增；upload_media >10MB 明确报错、>100MB 拒绝守护） |
| B10.2 QQ 适配器组装 | ✅ 已提交 | 15babe1：`gateway/qq/ws.rs`（WS 循环：Gateway URL→Hello→Identify/Resume→事件+心跳→重连；close code 分类/退避/快速断开检测；HTTPS_PROXY CONNECT 隧道 + rustls；`process_dispatch_event` 纯函数：群 @剥离/c2c/附件拼装/去重→ingest）；QqAdapter deliver 文本接线（truncate 分段已在核心层）；凭据 `PYLON_QQ_APP_ID`/`PYLON_QQ_CLIENT_SECRET` 配置化（run() 检测即注册+启动）；依赖新增 tokio-tungstenite 0.24/tokio-rustls 0.26/rustls 0.23/rustls-native-certs 0.8/futures-util；auth/events/send/types 接线部分 allow 移除（Identify/Resume/InboundEvent/引用/媒体/typing 预留仍标 allow）；+8 测试（dispatch/close code 分类） |
| B10.3 路由与会话生命周期 | ✅ 已提交 | 3a f877605：gateway 配置扩展（routes.allow_from/reset/idle_minutes + qq.group_allow_from）；白名单双控（群级+成员级，ingest_allowed 纯函数）；**ingest→ACP 发送接线**：send_prompt_core 公共管线（GUI/平台共用）+ ingest handler（binding.agent_id 路由/未绑定回退 active/目标懒连接 announce=false）+ dispatch_ingest（白名单通过后显式触发）；3b 6338b75：SessionInfo.updated_at + 活动时间更新（消息到达）+ expiry watcher（60s，idle 默认 1440/daily/off per-route；生成中 prompt 锁豁免；过期 close+移除+平台"[会话已重置]"通知+日志）+ session_expired 纯函数；+7 测试 |
| gateway 骨架 | ✅ 已提交 | `gateway/mod.rs` + 占位文件（施工位），53b0381 |
| B10.1 gateway 骨架接线 | ✅ 已提交 | PlatformAdapter trait + GatewayCore（b8257d7）：注册表/ingest 路由解析/deliver 分段分发（truncate 接入核心层）；事件广播双投（emit_event_all：WebView + 平台适配器，peri:update/done/error）；QqAdapter 骨架（handle_incoming 去重 → ingest）；**route/truncate/dedup 的 allow(dead_code) 已移除**（dedup 残留 2 个方法级 allow 待 B10.2 回复锚点接线）；+12 测试 |
| B9 权限审批流 | ✅ 源码完成（6fa20e2），待真实验收 | **B9.1 方向实证（Peri 源码）**：agent 主动发 request_permission（带 id 请求），客户端应答 RequestPermissionResponse（internally tagged `{"outcome": "selected"/"cancelled", ...}`）；cancel 时必须应答 Cancelled。实现：AcpClient::send_response；dispatcher 挂起（per-runtime pending_permissions）→ `pylon:permission-request` 事件（prompt 脱敏）；`approve_tool_call`/`set_approval_mode` 命令；模式 bypass/auto 自动批准、edit/default 挂起；超时 300s 默认拒绝；cancel/close 路径应答 Cancelled；fake ACP 全链路集成测试 |
| B11 注入钩子 | ✅ 已提交 | **B11.1 发送前置注入**：send_prompt_core（GUI 与平台 ingest 统一入口）发 prompt 前调 Prism `POST /inject {scenario, sources, user_msg, round}`（Prism main.rs handle_inject 实证；round 字段当前 Prism 保留未用 `_round`）→ context 前置拼进 prompt（空 context 原样）；round 每 session 计数（SessionInfo.inject_round，每次用户消息 +1，注入与持久化共用）；activated 透传 `peri:user.injectActivated`；Prism 不可用/请求失败 → 降级不注入（fail-open，消息照发）。**B11.2 完成持久化开关**：`gateway.inject.persist` = skip（默认）\| prism（POST /persist，Prism persist.rs 实证：回合文本经 dispatcher agent_message_chunk 流式收集到 SessionInfo.last_response_text，64KB 截断）。**B11.3 测试**：route 配置解析 5 测、Prism 请求形状桩 4 测、fake ACP 全链路集成 5 测（注入文本进 prompt 断言 + round 递增 + 禁用/不可用/命令消息降级 + persist 端到端） |
| B4.2 MCP 持久化 | ✅ 已提交 | `set_mcp_servers` 成功后原子写 `app_config_dir/pylon-mcp.json`（temp+rename，写失败 warn 不阻断）；启动 setup 加载（`load_mcp_persisted`：缺失/损坏/非法配置静默降级为空，内容过 validate_and_serialize 防手改注入；env/headers secret 保留在本地配置文件，不进日志/输出）；+4 测试（round-trip 含 secret、缺失/损坏降级、手改非法拒绝、命令写盘+清空落盘 `[]`） |
| 核验修复（全面核验） | ✅ 已提交 | **P1 修复**：自动重连闭包提前 return 路径（手动 reconnect 成功 / switch 走 / agent 缺失）不释放 `auto_reconnect_active` 防重入标志——下次崩溃将永远不再自动重连。修复：所有提前放弃路径先 `store(false)` 再 return；+1 回归测试（manual_reconnect_releases_auto_reconnect_flag）。**真实运行验收**：Peri 真实 ACP wire 全链路 PASS（initialize→new→流式 prompt→end_turn→close）；Prism 真实 /inject PASS（200 + 响应形状 + enabled=false 降级路径实证）；Hermes 协议面 PASS（initialize/new/MCP 形状），prompt 当前环境挂起（模型侧环境问题，非 Pylon 缺陷；Pylon 300s 超时+cancel 防御生效） |
| 核验修复（bug 批） | ✅ 已提交 | **Fix1**：会话过期 watcher 只对平台会话生效（适配器前缀或静态绑定命中判定），GUI local 会话豁免（不再被 idle 1440 分钟静默 close）；+1 测试。**Fix2**：MCP validate 同 server id==name 不再误报重复；+1 测试。**Fix3**：切 model 途径硬编码 agent id "hermes" → AgentDef 配置化 `set_model_api`（agents.yaml hermes 已配）；+1 测试。**Fix4**：QQ handle_incoming 白名单先于 seen 记录——被拒消息不占去重窗口，白名单放宽后重放可正常处理；+1 测试。另：adapter_keys 移除 allow(dead_code)（被 expiry watcher 消费） |
| B3 Prism route audit | ✅ 已提交 | **audit 结论**：39 命令路径/方法/query/body 逐一对照 Prism route（main.rs + mgmt/scenarios.rs + mgmt/blocks.rs + mgmt/entries.rs）全部真实，无伪能力命令（含 /api/blocks 块库与 /api/scenario/blocks 场景块的区分）；`prism_status` 走 /health 分类。**错误 DTO 统一**：39 个 prism_* 命令 `Result<_, String>` → `Result<_, PylonError>`（38 处 map_err(PylonError::Prism)，code=prism_error）；acp_rpc 保持 String（内部 helper） |

注意：gateway 三个文件（route/truncate/dedup）均有 `#[allow(dead_code)]`（未接线暂存），**B10.1 骨架接线时必须移除**。

### 1.2 工作区边界

混合状态：`src/`（前端，宫木云在改）、`references/`、`scripts/` 存在他人未提交改动。
后端只动 `src-tauri/**`、`agents.yaml`、本文。不执行 `git reset/clean/restore/add .`。

### 1.3 当前执行顺序（2026-07-31 定）

安全优先 → 纯后端可闭环 → 增量价值 → 大块按需。

| 序 | 大任务 | 说明 |
|:--|:--|:--|
| 1 | B9 权限审批流 | P0 安全核心；B9.1 先 wire 实测定方向 |
| 2 | B10 平台适配器层 | QQ 直连适配器；Prism 保持本地服务（注入插件） |
| 3 | B11 注入钩子 | ✅ 已提交（B11.1/2/3）——send 前置 /inject；GUI/QQ 同管线；待 CP-B11 真实验收 |
| 4 | B4.2 MCP 持久化 | ✅ 已提交——配置重启不丢 |
| 5 | B3 Prism route audit | ✅ 已提交——39 命令全真实，错误 DTO 统一 |
| 6 | B2 Inspector 完整版 | source 级 + Workspace/Runtime 增量 |
| 7 | B1.2 错误 DTO | ✅ 已提交（7fe8b94）：PylonError 结构化 `{code, message}`；核心命令已接入，Prism 命令待 B3 统一 |
| 8 | B5 Git 只读 | 前端 Changes/History 依赖 |
| 留档 | B7/B8 | 多 agent 并行、PTY/Runs/Git 写；架构大改，等前序验收 |

---

## 2. 源码地图

### 2.1 `lib.rs` — 主状态机 + Tauri 命令（~3,800 行）

| 区域 | 内容 |
|:--|:--|
| `AppState` | 全局 7 字段：runtimes（AgentRuntimeManager）/ agents / active_agent / pet / runtime_logs / runtime_mcp / prism；per-agent 字段全收进 `runtime::AgentRuntime` |
| `AgentRuntimeManager` | agent_id → AgentRuntime 注册表；`get_or_create` 懒启动单实例；`active_runtime` 按 active_agent 解析 |
| `AppStateHandles` | 全局字段子集（5 字段），供 async 闭包/静态辅助持有（Tauri state 不能 move）；per-agent 状态经 runtime 参数访问 |
| 通知 dispatcher | `start_notification_dispatcher(handles, runtime, window)`：绑定单个 runtime（generation 快照 + acp/sessions）；崩溃检测 + 自动重连 per-runtime 化——只重连本 runtime 的 agent，每轮复查 `status==Crashed` + `active_agent` 未切走（防重连成功把 active_agent 顶回） |
| 连接路径 | `do_connect_and_replace`（静态辅助，手动/自动共用，接收 runtime）→ `replace_agent_client`（keep_sessions 参数） |
| 切换语义 | `switch_agent`：目标 runtime `get_or_create` → 替换 → 先 abort 旧 runtime dispatcher 再 kill 旧 acp（防旧 dispatcher 收到崩溃通知触发重连）→ 旧状态置 Disconnected |
| 命令区 | Session/Prompt、Agent、Workspace/Runtime/MCP/Pet、Prism 四组（全表见 §4） |
| 运行入口 | `run()`：manage → invoke_handler → setup（宠物加载 + 按 active runtime 启 dispatcher）→ App::run（Exit 兜底持久化） |

关键不变量：客户端替换时本 runtime `client_generation` +1；dispatcher 与 session 映射按代际匹配；mutation 在 `.await` 后重验 mapping；命令层统一 `require_runtime()` 解析 active runtime。

### 2.2 `acp.rs` — ACP 客户端（~1,570 行）

- **方法常量**：9 个官方（initialize/new/prompt/close/load/list/set_mode/set_config_option/cancel）+ 1 个 Hermes 扩展（`session/set_model`，官方 schema 1.4 无此类型）
- **参数构造**：`session_*_params` 函数用官方 schema 类型序列化（`to_params`），MCP 走 `mcp::validate_and_serialize`
- **AcpClient**：`ManagedChild`（spawn + stdin/stdout/stderr）+ `write_tx`（mpsc 写通道）+ `next_id` + `pending`（16 分片 HashMap）+ `rx`（broadcast 256）+ `crashed`（AtomicBool）
- **锁外 RPC 模式**：`prepare_rpc` 同步产出 `PreparedRpc { id, line, write_tx, rx, pending }` → 发送/等待在锁外 → Peri 卡顿不阻塞其他命令
- **超时**：PROMPT_TIMEOUT 300s、CANCEL_SETTLE 30s、附件 10MB×8
- **reader**：stdout EOF → 广播 `pylon:agent-crashed` + drain pending（`ACP connection closed`）
- **child 清理**：`kill_and_wait` 三态（已退出/kill+wait/try_wait 失败兜底）；`Drop` 也 kill——任何错误路径不遗留子进程
- **fake 测试基建**：python 子进程脚本（`FAKE_SCRIPT` 模式：crash/alive），覆盖 initialize/new/prompt/EOF/cancel/load replay/malformed/stderr/延迟

### 2.3 `agent_runtime.rs` — 生命周期与路由纯逻辑（163 行）

- `AgentLifecycleStatus`：connecting/connected/reconnecting/crashed/disconnected/error
- `AgentRuntimeState`：status + last_error + last_connected_at
- 纯函数：`session_mapping_matches`、`source_for_peri_id_in_generation`（同代际唯一解析，多匹配拒绝）、`status_after_connection_failure`
- 自动重连常量：`MAX_RECONNECT_ATTEMPTS=5`、`reconnect_backoff_ms`（2^attempt 封顶 30s）

### 2.4 `agent_config.rs` — agents.yaml 解析

- `PYLON_AGENTS_CONFIG` 优先，否则内置 `agents.yaml`；`load_from_path` 支持显式路径
- `default_agent_id`：多个 default 拒绝；无 default 按 ID 字典序确定性回退
- `AgentDef`：name/transport/exe/args/cwd/env/default/set_model_api（切 model 途径）/model+acp_args（结构化 acp 参数，`command_args()` 展开）/`acp` 段（协议行为配置：initialize_caps 覆盖，差异字典外置）；env 含 secret 不进任何输出

### 2.5 `mcp.rs` — MCP 配置校验/序列化

- `McpServerConfig`：transport（stdio/sse/streamable-http/http）/URL/args/env/headers/OAuth/enabled
- 上限：MAX_SERVERS 32、args/env/headers 各 64
- `validate_and_serialize` → 官方 schema `SetSessionMcpServersRequest` 类型；secret 进 wire 不进日志
- B4.2 持久化在 lib.rs（`mcp_persist_path`/`persist_mcp_if_possible`/`load_mcp_persisted`）：pylon-mcp.json 原子写 + 启动恢复

### 2.6 `prism.rs` — Prism loopback 客户端

- `PrismClient`：固定 loopback（默认 `http://127.0.0.1:9337`，`validate_loopback_url` 白名单）、GET/POST/PUT/DELETE、query、Bearer（PRISM_ADMIN_API_TOKEN）、非 2xx 截断 4096、`unavailable` 降级
- B11：类型化 `inject()`（POST /inject → `InjectResult{context, activated, source}`，Prism 字段缺失容错）与 `persist_round()`（POST /persist，回合完成持久化）
- status 分类：connected/unavailable/unauthorized/configuration_error/error

### 2.7 `runtime_log.rs` — 运行时日志中心

- ring buffer 2000 条 + broadcast 256 + 过滤查询 + 递归脱敏（rawInput/rawOutput/prompt/persona/headers/env/authorization/token/secret）
- 消息截断 8KB

### 2.8 `workspace.rs` — 工作区只读接口

- root 由 `source → SessionInfo.cwd` 解析；`resolve_workspace_path` canonical containment（穿越/前缀/UTF-8/NUL 检查）
- 目录上限 1000 条；预览默认 256KB / 最大 1MB

### 2.9 `error.rs` — PylonError

- 枚举：Acp/SessionNotFound/NoActiveAgent/Workspace/Prism 等；`Serialize` 输出结构化 DTO `{code, message}`（B1.2 已接入核心命令；**B3 已统一 Prism 命令**——39 个全部 PylonError::Prism，code=prism_error）

### 2.10 `pet.rs` + `pet-core/` — 宠物

- `pet-core` 独立 crate（366+ 行）：事件驱动状态机，5 阶段（XP 0/25/90/220/500）、7 情绪、记忆 10 条（里程碑：首任务/每10次/进化/7天）、睡眠（首字 30s 无互动入睡、访问唤醒）、TokenDelta 预留
- `pet.rs` 适配层：事件转接 + `PetView` DTO + 落盘持久化（`save_to_file` 原子写 / `load_from_file` 容错读）
- 落盘：`app_config_dir/pylon-pet.json`；写时机 get_pet（12s 轮询）+ pet_action + RunEvent::Exit

### 2.11 `main.rs` — 入口

`run()` 调用 lib；Tauri context 在 build.rs 注入 comctl32 v6 manifest（windres .o，测试 exe 兼容）。

---

## 3. ACP 协议实现面

### 3.1 方法面

| 方法 | 常量 | 用途 |
|:--|:--|:--|
| initialize | `METHOD_INITIALIZE` | 握手（统一带 `_meta.peri.*`，Hermes 忽略无害） |
| session/new | `METHOD_SESSION_NEW` | 建会话（cwd + mcpServers） |
| session/prompt | `METHOD_SESSION_PROMPT` | 发消息（content blocks 类型化） |
| session/close | `METHOD_SESSION_CLOSE` | 关会话 |
| session/load | `METHOD_SESSION_LOAD` | 重放历史（replay-before-response） |
| session/list | `METHOD_SESSION_LIST` | 常量已备，未接线命令 |
| session/set_mode | `METHOD_SESSION_SET_MODE` | 切换模式 |
| session/set_config_option | `METHOD_SESSION_SET_CONFIG_OPTION` | 配置项（Hermes 只认 edit_approval_policy） |
| session/cancel | `METHOD_SESSION_CANCEL` | 取消 prompt（settle 30s） |
| session/set_model | `METHOD_SESSION_SET_MODEL` | **Hermes 扩展**（unstable，官方 schema 无） |
| session/request_permission | — | **B9 待实现**（官方类型已备） |

### 3.2 通知面

| 通知 | 处理 |
|:--|:--|
| session/update | dispatcher 主通道：agent_message_chunk / user_message_chunk / usage_update / tool_call / tool_call_update / session_info_update / config_option_update；未知 variant 白名单跳过 |
| pylon:agent-crashed | **内部通知**（非 ACP 标准）：stdout EOF 时广播 → 置 Crashed → 自动重连调度 |

`session/update` 仅携带 sessionId → `source_for_peri_id_in_generation` 按当前代际唯一解析；无匹配/多匹配丢弃并告警。

### 3.3 差异适配表（差异字典外置，2026-08 核验重构）

**配置化差异**（agents.yaml per-agent，无需改代码即可热导入/适配新 agent）：

| 差异点 | 配置字段 | 说明 |
|:--|:--|:--|
| 切 model 途径 | `set_model_api: true` | Hermes 系走 session/set_model（unstable）；缺省 false = set_config_option（Peri 官方路径） |
| initialize clientCapabilities | `acp.initialize_caps: {...}` | 原样进 wire；缺省 = 统一默认（tokenStats + `_meta.peri.*`，Hermes 忽略无害） |

**统一兼容策略**（不配置，客户端对两边都安全）：

| 差异点 | Peri (schema 1.4) | Hermes (0.11.2 / 0.18.2) | Pylon 策略 |
|:--|:--|:--|:--|
| mcpServers 字段 | 容忍缺失/空（DefaultOnError） | 必填 List（缺字段即拒） | 总是带空数组/全量字段 |
| session/close | 支持 | 旧版本无（-32601） | 总是调用 + 降级本地清理 |
| initialize `_meta.peri.*` | 认 | 忽略无害 | 统一携带 |

结论：wire 分叉仅切 model 途径（已配置化）与 initialize caps（可覆盖）；其余为统一兼容策略。

---

## 4. Tauri 契约

### 4.1 commands 全表（generate_handler 实注册）

Session/Prompt：`new_session` `send_message` `set_mode` `set_config_option` `close_session` `cancel_prompt` `load_sessions` `session_inspector` `load_persisted_session` `list_persisted_sessions` `export_session`

Agent：`list_agents` `switch_agent` `reconnect_agent` `agent_status` `reload_agents` `validate_agents`

Workspace/Runtime/MCP/Pet：`get_workspace_root` `list_workspace_entries` `read_workspace_text` `list_runtime_logs` `clear_runtime_logs` `push_frontend_log` `set_mcp_servers` `get_pet` `pet_action`

B9 权限：`approve_tool_call` `set_approval_mode`

Prism（39）：`prism_health` `prism_status` `prism_state` `prism_scenarios` `prism_sources` `prism_aliases` `prism_config` `prism_logs` `prism_chronicle` `prism_history` `prism_scenario` `prism_blocks` `prism_inject` `prism_command` `prism_create_scenario` `prism_delete_scenario` `prism_create_source` `prism_delete_source` `prism_source_detail` `prism_source_files` `prism_read_source_file` `prism_write_source_file` `prism_delete_source_file` `prism_source_entries` `prism_source_entry` `prism_add_source_entry` `prism_edit_source_entry` `prism_delete_source_entry` `prism_update_config` `prism_update_scenario` `prism_create_block` `prism_update_block` `prism_delete_block` `prism_add_scenario_block` `prism_edit_scenario_block` `prism_delete_scenario_block` `prism_reorder_scenario_blocks` `prism_reload` `prism_llm_test`

### 4.2 events 全表

`peri:user`（用户消息回显，含 replay 标记；B11 注入激活时带可选 `injectActivated` 数组）· `peri:update`（session/update 转发，带 source）· `peri:done` · `peri:error` · `peri:agent-status`（agentId/agentName/status/generation/active/available/lastError）· `pylon:runtime-log` · `pylon:permission-request`（B9：requestId/sessionId/toolCallId/title/prompt/options/requestedAt）

### 4.3 session_inspector 契约

```json
{ "agent": { "id", "status", "lastError", "lastConnectedAt" },
  "summary": { "sessionCount", "activeCount", "tokensTotal", "tokensIn", "tokensOut" },
  "sessions": [{ "source", "periId", "title", "model", "mode", "tokensIn", "tokensOut", "tokensTotal", "contextSize", "cwd" }] }
```
`activeCount` = `has_first_prompt` 会话数（保守近似；`last_activity_at` 留档 P2）。

### 4.4 pet_action 动作集

`poke` / `feed` / `rename` / `daily` / `sleepy` / `nostalgia` / `restore`。返回 PetView + 可选 `sleepy`/`msg` 字段。

### 4.5 Agent 配置形状

```yaml
agents:
  peri:
    name: Peri
    transport: subprocess
    exe: F:\A-I\Agent\Peri\target\release\peri.exe
    args: ["acp"]                    # 进程级参数（含入口子命令 acp）
    model: deepseek-v4-flash         # 结构化：展开为 --model <value>，追加在 args 后
    acp_args: ["--verbose"]          # 结构化：附加任意 acp 参数，追加在 args 后
    cwd: G:\Project\prism-desktop
    default: true
  hermes:
    name: Hermes
    transport: subprocess
    exe: hermes
    args: ["acp"]
    cwd: G:\Project\prism-desktop
    set_model_api: true   # 可选：切 model 走 session/set_model（Hermes 系；缺省 false = set_config_option）
  custom:
    name: CustomAgent
    transport: subprocess
    exe: D:\agents\custom.exe
    args: ["acp"]
    acp:
      initialize_caps:    # 可选：覆盖 initialize 的 clientCapabilities（原样进 wire）
        fs: {}
        auth: {}
        _meta:
          "peri.skillNames": true
```
`env` 可能含 secret，不得通过 list/status、RuntimeLog、错误摘要返回。
命令行拼接顺序：`exe + args + ["--model", model]? + acp_args`（结构化参数在后，多数 CLI 后者覆盖前者；只写 args 的老配置行为不变）。

### 4.6 Prism 配置

`PYLON_PRISM_URL`（可选，loopback 白名单）· 默认 `http://127.0.0.1:9337` · `PRISM_ADMIN_API_TOKEN`（写接口 Bearer，不进前端/日志/文档）。读/写接口认证以 Prism 源码为准；不得新增通用 `prism_request(url, method, body)`。

---

## 5. 关键机制

### 5.1 generation 路由与 session 映射

```
source ── SessionInfo { peri_id, generation, ... } ── 映射
client_generation = 客户端替换计数（每次替换 +1）
dispatcher 持 generation 快照；session/update 按 (peri_id, generation) 唯一解析 source
```

- 旧代际事件：`session_mapping_matches` / `source_for_peri_id_in_generation` 拒绝（log warn，不取第一个）
- 客户端替换后旧 session 映射若迁移 generation 仍有效（自动重连续聊），否则前端按错误清理

### 5.2 崩溃自动重连

```
stdout EOF → pylon:agent-crashed → dispatcher 置 Crashed + emit
→ auto_reconnect_active.swap(true) 防重入
→ 闭包循环 5 次：sleep(2^attempt s 封顶 30s)
   → 复查 status==Crashed（用户手动 reconnect 则放弃）
   → 复查 generation 未变
   → do_connect_and_replace(Reconnecting, keep_sessions=true)
→ 成功 break / 全败 Release 标志 + log error
```

### 5.3 锁外 RPC（V14 模式）

所有 RPC（new/prompt/set_mode/set_config_option/cancel/close/load）统一：短锁内 `prepare_rpc`（同步准备）→ 锁外发送 + 等待。Peri 卡顿时其他命令不阻塞。响应通过 `AcpClient::complete_rpc` 或 `PromptWaitOutcome` 结算。

### 5.4 child 生命周期

- spawn 后 stdin/stdout/stderr 全部接管；stdout reader 线程广播消息
- 退出清理：`kill_and_wait`（已退出直接返回；否则 kill + wait；try_wait 失败兜底继续 kill）
- `Drop` 也清理——AppState 销毁链自动收尾，无孤儿进程
- 附件：10MB × 8 上限；`infer` 类型探测

### 5.5 宠物（事件源与持久化）

事件源：聊天（发送/首字/完成/失败/token 全量/工具三态）· 互动（poke/feed/rename/每日访问）· `get_pet` 轮询自动入睡 · Agent 连接成功/崩溃。落盘：`app_config_dir/pylon-pet.json` 原子写；写时机 get_pet + pet_action + Exit；启动加载容错（缺失/损坏→新宠物）。

---

## 6. 能力状态表（B 任务体系）

| 大任务 | 状态 | 说明 |
|:--|:--|:--|
| B0 启动配置/Registry/降级 | 🟡 源码完成，待真实验收 | validate_agents、deterministic default、Prism unavailable 降级 |
| B1 ACP 生命周期/隔离/自动重连 | 🟡 源码完成（含自动重连+核验修复），待真实验收 | mutation 身份重验已实现（ensure_generation + with_session_if_matches 全量代际校验） |
| B2 Workspace/Inspector/Runtime | 🟡 基础完成，Inspector v1 已实现 | 完整版 source 级聚合待做 |
| B3 Prism 适配 | 🟡 route audit + 错误 DTO 统一已提交 | 真实验收缺失（V-B3：真实 Tauri invoke、真实 Prism 临时数据 CRUD、token 传播） |
| B4 Export/MCP/Settings | 🟡 Export 完成，MCP 持久化已提交（B4.2） | 真实验收缺失（V-B4） |
| B5 Git 只读 | ❌ 待开发 | 固定 runner → status → diff → history |
| B6 Hermes 兼容 | 🟡 wire 实测通过 | 剩 request_permission（并入 B9）|
| B7 多 Agent 并行 | ❌ 留档 | 架构大改 |
| B8 PTY/Runs/Git 写 | ❌ 留档 | 前置 B5/B7 |
| B9 权限审批流 | ❌ P0 已排期 | B9.1 wire 实测定方向 → 挂起 → 模式判定 → 契约 → 集成测试 |
| B10 平台适配器层（Gateway） | 🟡 骨架+QQ+路由+会话生命周期已提交 | QQ 适配器直连；Prism 保持本地服务（不嵌入） |
| B11 注入钩子（消息管线） | 🟡 源码完成（B11.1/2/3 已提交），待真实验收 | 发送前置 /inject → context 拼 prompt；来源无关（GUI/QQ 同管线）；CP-B11 真实 Prism 数据目录注入验收缺失 |

验收规则：focused test 通过只能更新"源码已有"列；真实 Tauri/Peri/Prism/process/artifact 证据齐全才"已验收"；curl/pure test/fake ACP/cargo check 不得替代真实证据。

## 6.1 待开发任务详章（B9-B11）

### B9 权限审批流（tool-call 批准 + session/request_permission）

业务结果：agent 工具调用按前端权限模式（bypass / auto / edit / default）放行、自动或询问用户；不悬挂 pending；前端可显式批准/拒绝并记忆选择。

当前源码事实：

- 前端已有权限模式 UI，后端只把模式透传为 `edit_approval_policy` 配置；**批准交互流完全空缺**——dispatcher 收到 tool_call 通知后只转发 + 喂宠物，无挂起、无响应。
- 官方 schema（agent-client-protocol-schema 1.4）已有批准类型：`session/request_permission`（v1+v2，x-side=client——GUI 是调用方）、`RequestPermissionRequest { sessionId, prompt, subject, options }`、`PermissionOption`（AllowOnce / AllowAlways / RejectOnce / RejectAlways）、`RequestPermissionResponse { outcome }`。
- **方向语义待 wire 实测**：request_permission 由 GUI 主动调用询问 agent，还是 agent 在 tool_call 通知后挂起等 GUI 授权——需要 fake + 真实 Peri/Hermes 确认。

B9.1 批准方向 wire 实测 → B9.2 挂起与恢复（tool_call 挂起 → 用户决定 → 继续/中止；超时默认拒绝）→ B9.3 模式判定（bypass/auto/edit/default）→ B9.4 前端契约（`approve_tool_call` 命令 + `pylon:permission-request` 事件，安全字段不含 secret）→ B9.5 集成测试（fake 批准流；拒绝/超时不悬挂）。

非目标：不实现 fs/network 工具面；不实现权限 UI 本体（前端）。出口：CP-B9 真实 Tauri 弹窗 + 真实 Peri/Hermes 批准 wire。

### B10 平台适配器层（Gateway）

业务结果：Pylon 从"桌面 GUI"扩展为"消息网关"——多平台消息（QQ/微信/飞书/ins 等）经适配器接入，统一经 ACP 路由到本地 agent；本地 GUI 与平台消息同管线；Prism 保持本地服务（上下文注入插件），不做平台桥接。

```text
QQ ──┐
微信 ├─→ Gateway(Pylon) ──ACP── 本地 Agent（Peri/Hermes 子进程）
飞书 ├─   平台适配器层           stdin/stdout JSON-RPC
ins ─┘   │
         ├─ gateway/mod.rs  PlatformAdapter trait + 注册表
         ├─ gateway/qq.rs   QQ 适配器（第一实现）
         └─ 注入钩子 ──→ Prism(9337, 本地服务插件)
```

消息拓扑：`平台(qq/微信/飞书/ins) → gateway → ACP → 本地 agent`。信息脉络：`agent ←ACP→ Pylon(gateway) → 平台`；Prism 是附加插件（上下文注入），不承担平台桥接（其 qq 模块闲置）。

当前源码事实：

- 前端 Session 已有 `platform` 字段（'local'/'qq'），source 是字符串路由 key，可扩展 `qq:group:{id}` / `qq:user:{openId}`。
- 后端无平台适配器抽象；消息只来自 GUI 输入（send_message）；agent 事件只发 WebView（无平台投递通道）。
- Prism 的 qq 模块（ws.rs/types.rs/events.rs/send.rs/auth.rs）可作移植参考：QQ 开放平台 WS gateway + 事件解析 + v2 API 发送。
- QQ 凭据：app_id + client_secret（Prism 里经 /qq/connect 传入；Pylon 改为配置 `PYLON_QQ_APP_ID` / `PYLON_QQ_CLIENT_SECRET` 或 gateway 配置段）。

B10.1 PlatformAdapter trait + gateway 框架（ingest/deliver + 注册表；事件广播改造：peri:update/done/error 从"只发 WebView"改为"广播到 WebView + 各平台适配器"）→ B10.2 QQ 适配器移植（WS 连接/事件解析/发送；凭据配置化；Prism 零改动）→ B10.3 静态绑定路由（EntityBinding 配置表：platform_key → agent/profile/session；预留动态覆盖接口）→ B10.4 集成测试（fake QQ 事件流 → ingest → 注入钩子 → fake ACP → deliver 回发）→ B10.5 指令切换（二期，预留接口）。

### 绑定与路由设计（B10.3 核心）

首版静态绑定；接口预留动态切换。

```text
EntityBinding { agent_id, profile_id, session_key }   // 静态配置首版
route 表：platform_key（qq:group:123）→ EntityBinding
  未配置实体 → 默认绑定（active_agent + 默认 profile）

预留动态覆盖（二期）：
  route 表 = 静态配置 ⊕ 动态覆盖（持久化，重启不丢）
  指令：/agent <id> /profile <id> /session <k> /bind /status
  权限：allowed_admin_ids（per-platform 白名单，未授权忽略/提示）
```

- 切换粒度 = 实体级（群级/用户级）；切换 agent 时目标懒启动、原 agent 会话现场保留（per-runtime sessions 隔离）
- profile 语义 = persona/人设（对应 session/new 的 persona 参数，前端 Session.profileId 同维度）

### 长回复切断方案（B10 deliver）

平台单条文本上限：QQ = 4000 字符（Hermes `MAX_MESSAGE_LENGTH` 实证）。agent 输出超限必须切断。

```text
deliver 文本管线：完整输出 → 分段器 → 串行发送（失败中止 + 告警）
分段规则（参考 Hermes truncate_message）：
  1. 超限才切（≤上限原样发）
  2. 代码块 fence 保留：切段若在 ``` 内 → 段尾闭 fence + 下段用原语言标签重开
  3. 分段指示器 "(1/N)"（预留 10 字符预算）
  4. 自然切点：优先换行 → 空格 → 硬切
  5. len_fn 可插拔（UTF-16 单位——Telegram 类平台扩展用）
```

首版实现完整规则（agent 输出代码高频，fence 保留必须做）。

### 重放去重与消息序列（B10.2 QQ 适配器）

QQ WS 断线重连后 resume 会重放事件——必须去重，否则 agent 收到重复消息。

```text
去重（参考 Hermes _seen_messages 滑动窗口）：
  seen: msg_id → ts，上限 DEDUP_MAX_SIZE = 1000（Hermes 实证），超限清理旧条目
  resume 重放事件 msg_id 已见 → 丢弃，不 ingest

消息序列：
  last_msg_id: chat_id → 最新 msg_id（回复锚点 + msg_seq 计算，QQ 回复 API 必需）
  注：Prism qqbot 已有 last_msg_id 雏形（按用户存），需改为 chat 级 + 补 seen 窗口
```

去重在适配器层做（平台细节），ingest 层只见干净消息。

### 速率控制（B10 deliver）

```text
发送队列：agent 输出排队串行发送（与切断方案共用，天然节流）
rate_limited（平台限流）：等待 RATE_LIMIT_DELAY = 60s 重试 → 超限放弃告警
HTTP 连接池上限（复用连接，防 fd/连接泄漏，Hermes _http_client_limits 模式）
```

### 触发与回复语义（B10.3）

平台会话的消息触发条件与回复目标（参考 Hermes `_handle_group_message`/`_handle_c2c_message`）。

```text
群聊（group）：
  仅 @bot 消息触发（剥离 @ 前缀——移植 Prism events.rs strip_at_mention）
  回复回全群（chat_id = group_openid）
  白名单双控：群级 group_openid + 成员级 member_openid（可配置，Hermes group_allow_from 模式）

私聊（c2c）：
  消息即触发，回复回本人（chat_id = user_openid）

触发时附件处理：图片 URL / 语音转写 / 附件描述拼进文本（Prism events.rs 可移植）
@ 剥离、白名单、附件处理都在适配器层完成，ingest 只见干净文本

### 媒体出入站（B10.2 QQ 适配器）

```text
入站（用户发媒体 → prompt）：
  附件解析（移植 Prism events.rs：图片 URL / 语音占位 / 附件描述）
  + 大小上限 128MB + 类型白名单 + 临时目录清理（Hermes validate_inbound_media_size 模式）
  语音转写首版占位（QQ asr_refer_text 后续）

出站（agent 生成媒体 → 平台）：
  首版：agent 输出媒体 URL → 直接发文本 URL（降级，简单可靠）
  二期：图片/文件上传（chunked_upload + file_info + 瞬时重试 + 上传缓存，Hermes _upload_media 模式）
```

### 发送失败处理（B10 deliver）

```text
瞬时失败（网络/429）：有限重试（3 次指数退避）→ 最终告警（不静默丢）

死目标（二期，Hermes dead_targets 模式）：
  错误分类 forbidden/not_found（群被删/拉黑/注销）→ 标记 target 不可达 → 短路投递 + 告警
  成功发送清除标记（自愈，无需人工清理）
  首版：重试 3 次 + 日志告警；死目标注册表留接口
```
```

### 会话超时与重置（B10.3）

```text
策略（参考 Hermes reset policy）：
  模式：idle（updated_at + idle_minutes，默认 1440=1 天）/ daily（每日重置）/ 关闭（per-route 可配置）
  活跃豁免：agent 生成中/工具执行中的会话永不视为过期（防杀正在运行的任务）
  后台 expiry watcher 定期检查 + 过期 close agent session + 用户可见重置原因提示
```

### 运维与边界（B10 收尾）

```text
状态查询：新命令 gateway_status —— 各平台连接状态/最近活动/路由表（对齐 agent_status 模式）
配置热重载：路由表/凭据改动 → reload_gateway 命令（首版重启生效，命令预留；Hermes config 模式）
入站长截断：ingest 入站消息长度上限（如 64KB）截断再进 prompt（防超长消息打爆 agent 输入）
附件安全：#5 已覆盖（128MB 上限 + 类型白名单 + 临时目录清理）
```

非目标：不把 Prism 代码嵌入 Pylon；QQ 为第一实现，微信/飞书/ins 适配器后续按同一 trait 扩展；指令切换二期（本版只留接口）。出口：CP-B10 真实 QQ 机器人 wire（或用户授权跳过）。

### B11 注入钩子（gateway 消息管线）——✅ 源码完成（待真实验收）

业务结果：本地 GUI 与平台适配器消息统一走"发送前置注入"——调 Prism `/inject` 拿 context，拼进 prompt 后经 ACP 发给 agent；生成后持久化可配置。

```text
用户消息（GUI / QQ 适配器）
  → 注入钩子: /inject { scenario, sources, user_msg, round } → context + activated
  → prompt 组装: [注入 context] + 用户消息 → ACP session/prompt → agent → LLM
  → 完成（可选）: /persist { scenario, sources, user_msg, response, round }（Prism 摘要持久化）
```

当前源码事实（B11 已实现，2026-07-31）：

- **配置**（`agents.yaml` `gateway.inject` 段，route.rs `InjectConfig`）：`enabled`（默认 true，Prism 不可用自动降级不注入）/ `scenario` + `sources`（可选，缺省让 Prism 用 active）/ `persist`（`skip` 默认 | `prism`）。
- **接线点**：`send_prompt_core`（GUI `send_message` 与 gateway ingest 统一入口）；命令消息（`/` 开头）不注入（与 persona 拼接规则一致）。
- **round 语义**：SessionInfo.inject_round，每次用户消息 +1（首条 0），注入与持久化共用；Prism 当前版本 round 为保留字段（`_round` 前缀未消费）。
- **activated 透传**：`peri:user` 事件可选字段 `injectActivated`（激活条目 uid 列表）。
- **完成持久化**：`persist=prism` 时回合完成后 POST `/persist`（user_msg + dispatcher 流式收集的回复文本，64KB 截断；空回复跳过）；失败只告警不阻断。
- **降级**：Prism configuration_error / 请求失败 → 不注入不持久化，消息照发（fail-open）。

非目标（未做）：system/persona 级注入（第一版 prompt 前置注入）；改 Prism（inject/persist 均为 Prism 现成接口）。出口：CP-B11 真实 Prism 数据目录注入验收（G:\Prism 临时 root）——缺失。

---

## 7. 开发流程

### 7.1 铁律

1. 源码优先：Pylon Rust、Peri handler、Prism route 是事实源；旧文档/skill 只定位
2. 后端边界：只动 `src-tauri/**`、fixture/测试、`agents.yaml`、本文
3. 保留混合工作区：不 reset/clean/restore/add .；不覆盖他人 staged/unstaged/untracked
4. 安全优先：generation 隔离、pending 收敛、child kill/wait、路径 containment、loopback、secret 脱敏
5. 不制造伪可用能力：DTO/command 注册/mock 不得写成真实链路完成
6. 一条一 commit：一个可验收切片一个 commit；cargo check 通过再 commit；不提前 build、不跳级
7. 文档与源码同步：修改后旧验证证据视为 stale

### 7.2 验证命令

```bash
# 接手
git status --short --branch && git log -5 --oneline && git diff -- src-tauri docs/后端开发与交接手册.md

# 代码后（按变更范围执行）
# 注意：本机工具链已迁移（2026-08 核验）——原 F:/Coding/rust/toolchain/.cargo 不存在。
# 正确环境：
#   cargo/rustc: F:\Coding\rust\.rustup\toolchains\stable-x86_64-pc-windows-gnu\bin
#   gcc/windres: F:\Coding\rust\mingw64\bin
#   CARGO_HOME 必须显式设为 F:\Coding\rust\.cargo（环境变量原指向已删除的 toolchain 目录，
#   否则 --offline 找不到依赖）
export PATH="/f/Coding/rust/.rustup/toolchains/stable-x86_64-pc-windows-gnu/bin:/f/Coding/rust/mingw64/bin:$PATH"
export CARGO_HOME="F:/Coding/rust/.cargo" && export TMP=F:/tmp && unset RUSTFLAGS
cd src-tauri && cargo check --offline && cargo test --offline --lib
cd .. && npm run build   # 前端未完成改动会挡，区分归属

# 注意
# - GNU 工具链，无 MSVC；RUSTFLAGS 必须 unset（lld 替代 MinGW ld 会 link 失败）
# - 测试 exe 依赖 windres 注入 manifest（build.rs 已处理）
# - 0xc0000139 启动错误 = harness 阻塞，不得写成测试通过
```

### 7.3 接管顺序

1. 刷新 Git 状态/branch/最近 commit/限定 diff
2. 对照 §6 确认目标大任务状态未被工作区改动改变
3. 读任务所需 Pylon 源码；协议任务同时读 Peri/Prism 真值
4. 记录外部改动归属，不覆盖、不 reset、不全量 stage
5. 选最高优先级未阻塞任务；"继续"时直接进入，不重复询问
6. 完成适用验证 → 更新本文 §6 → 一条一 commit → 停下汇报

---

## 8. 验收区（缺真实证据清单）

| 验收项 | 源码已有 | 缺失证据 |
|:--|:--|:--|
| V-B0 Agent 配置/降级 | 配置来源、deterministic default、registry/status、validate_agents、Prism unavailable | fresh 编译、外部配置启动、坏配置保留旧状态、无 Agent/坏 Prism 真实桌面 |
| V-B1 ACP 生命周期 | pending、EOF、cancel、load replay、generation、fake ACP、崩溃自动重连集成测试 | event-level A→B→A、真实 Peri JSONL、真实 WebView event、Windows PID/process |
| V-B2 Workspace/Runtime | containment、preview、ring buffer、redaction | Windows junction/race、分页、真实 source 切换、真实导出文件 |
| V-B3 Prism | loopback client、unavailable、命令注册、TCP fixture | route 全量对照、真实 Tauri invoke、真实 Prism 临时数据 CRUD、token 传播 |
| V-B4 Export | owner、sanitizer、原子写、拒绝覆盖 | 真实 replay、read-back、secret probe、stale generation race |
| V-B4 MCP/Settings | 输入校验、serialization、mode/config wire、**持久化（B4.2：原子写+启动恢复+损坏降级）** | 真实 Peri、失败回滚、secret 全链 |
| V-Pet | 命令、前端入口、后端落盘（原子写+启动加载+Exit 兜底） | 真实跨重启 persistence、非法 restore |

发行边界：`G:\Prism` 是运行数据目录非源码；生产数据不做 CRUD fixture；token 不进 agents.yaml/frontend/log/本文。

---

本文不是"后端全部完成"或"系统已上线"声明。真实 Tauri、Peri、Prism、Windows process、导出文件证据缺失时，只说明对应源码/测试层完成。
