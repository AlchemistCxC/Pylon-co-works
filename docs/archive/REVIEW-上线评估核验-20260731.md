# 上线评估文档核验报告

> 核验日期：2026-07-31
> 核验对象：`Pylon项目现状与上线距离评估.md`（2026-07-31 版，735 行）
> 核验方式：全文档通读 + 源码 grep/read + 实测编译/测试
> 核验人：Riccati（审计）

## 结论先行

评估文档写作时间约在 07-31 中午前（其 git 基线为 2c766bd / ahead 7）。此后 main 前进 8 个 commit 至 868b0be（14:02），**文档三大硬阻塞已全部消除**：

1. ❌→✅ Rust 编译断裂（§4.1）——已修复，`cargo check` 通过
2. ❌→✅ 前端回归失败（§4.2）——已修复，`npm run test:frontend` 全绿
3. ❌→✅ release exe 过期（§4.3）——已重建（07-31 12:55，34.6MB）

真正剩余阻塞与文档判断一致：无安装包、发布配置未收口、真实 Tauri+Peri 主链验收缺失、Prism 前端未接入、Git/Activity/Runtime Sheet 未实现。**"当前不能上线"的结论仍成立**，但完成度应上调（内部 Alpha 约 65% → 约 80%）。

## 一、编译 / 测试 / 回归（§4.1-4.3）

| 文档断言 | 实测结果 | 判定 |
|:--|:--|:--|
| Rust 无法编译，lib.rs:1177/1284/1298 缺 `session_matches`（exit 101） | `cargo check` 通过（11.6s），仅 1 个 dead_code warning（workspace.rs WorkspaceError 变体） | 🔴 过时——已修复（V17 P0 补 `session_matches`） |
| `cargo test --lib --no-run` 同样失败 | 通过（23.8s，2 warnings，unittests 产出 exe） | 🔴 过时——已修复 |
| 前端回归失败：test-scroll-bottom-offset.mts 断言 `--right-panel-inset` 在 App.tsx | `npm run test:frontend` EXIT 0 全绿（52s）；scroll-bottom-offset 基线已修复，断言移至 AgentSheetView.tsx | 🔴 过时——已修复 |
| release exe 过期（07-30 18:36, 35,239,571 B） | pylon.exe 07-31 12:55, 34,676,374 B；HEAD 14:02 的 868b0be 仅改前端 8 文件（零 Rust 改动）→ exe 对当前 Rust 源码有效 | 🔴 过时——已重建 |
| 无安装包（MSI/NSIS/bundle） | `target/release/bundle/` 不存在 | ✅ 成立 |
| 无 CI/release workflow | `.github/workflows/` 不存在 | ✅ 成立 |

## 二、发布配置（§4.5）

| 文档断言 | 实测结果 | 判定 |
|:--|:--|:--|
| `csp: null` + `dangerousDisableAssetCspModification: true` | tauri.conf.json `security` 实为二者皆设 | ✅ 成立 |
| capabilities: shell:default / dialog:default / fs:default | capabilities/default.json 三权限齐全 | ✅ 成立 |
| 无 updater / 签名 / publisher / release targets | tauri.conf.json 无相关键 | ✅ 成立 |

## 三、主链能力声称（§3，文档称已具备——全部核实成立）

| 声称 | 源码证据 | 判定 |
|:--|:--|:--|
| 12 个核心 Tauri commands | lib.rs:1736 invoke_handler：new_session/send_message/set_mode/set_config_option/close_session/cancel_prompt/load_sessions/list_agents/switch_agent/reconnect_agent/agent_status/reload_agents + load_persisted_session/export_session 等 | ✅ |
| Prism 桥接命令已注册（§3.6） | 同 invoke_handler：prism_health 至 prism_llm_test 约 40 个 | ✅ |
| 前端监听 peri:user/update/done/error/agent-status | ChatView.tsx:211/247/335/353 + App.tsx:76 | ✅ |
| agents.yaml 配置 Peri（default）+ Hermes | agents.yaml：peri default:true；hermes exe=hermes | ✅（注：peri args 实际为 `["acp","--model","deepseek-v4-flash"]`，文档写 `["acp"]` 已过时——07-31 配了 V4 模型） |
| 多个 default 拒绝加载 / 运行时 reload / 降级启动 | agent_config.rs:87-89 多 default 返回 Err；reload_agents 已注册；lib.rs:1055 fallback_status | ✅ |
| Workspace 路径安全七措施（§3.4） | workspace.rs：拒绝绝对路径(L54)、拒绝 `..`(L60 TraversalRejected)、canonicalize containment(L70-77)、symlink 检测(L123/138)、预览 1MB 上限、目录 1000 条目上限(L145) | ✅ |
| RuntimeLog 脱敏（§3.5） | runtime_log.rs:134-154：敏感字段 12 类 + REDACTED 替换 + 单测(197-244) | ✅ |
| PrismClient 安全（loopback 限制/token/超时/截断） | prism.rs 实现 | ✅ |

## 四、缺口清单（§5-6）——多数成立，两处过时

| 文档断言 | 实测结果 | 判定 |
|:--|:--|:--|
| SheetHost 仅 agent/prism 真实 renderer，runtime/file/diff/changes/git-history 显示"尚未接入" | SheetHost.tsx:29/31 仅两 case；L56-57 "尚未接入" | ✅ 成立 |
| main 右栏仍为 workspace/logs/reserved-1/reserved-2 | RightPanel.tsx:26 已为四 Tab workspace/logs/activity/changes（91c9864 已 merge 进 main，9eb1632） | 🔴 过时——已集成 |
| activity/changes 为 unavailable 占位 | RightPanel.tsx:164/169 PanelStatus empty | ✅ 成立 |
| PrismSheet 静态演示，操作禁用 | PrismSheet.tsx:16 "演示预览：尚未接入 Prism API" | ✅ 成立 |
| Git 后端无（BE-B5-001 未开始） | 无 git 相关 Rust 模块 | ✅ 成立 |
| MCP runtime-only 进程内 snapshot | lib.rs:835 current_mcp_servers 读内存 state，无磁盘持久化 | ✅ 成立 |
| 任务卡 14 张，main 2/14，含 Worker 3/14 | .agents/tasks/index.yaml 确为 14 张；kanban 实际同步 1 张（t_5c6f3f55 FE-F1-001 done）；BE-B0-001 已进 main（809f793）、FE-F0-008 已进 main（c1b7462） | 🟡 部分过时——实际已完成 3/14 进 main |
| git 基线 HEAD 2c766bd / ahead 7 | HEAD 868b0be / ahead 15 | 🔴 过时 |

## 五、其他核对

- 行数：前端 TS/TSX/CSS 10,640 与 skill 记录 8,949+1,691 一致 ✅；Rust 4,711 ✅
- 版本号 package.json / Cargo.toml / tauri.conf.json 均 0.1.0 ✅
- 窗口标题运行期由 setup 设 "Pylon"（lib.rs setup set_title）✅

## 六、当前真实阻塞清单（核验后）

按文档 §7.1 Alpha 标准，剩余项：

```
P0  真实 Tauri + Peri 主链验收（new/send/stream/cancel/close/load/A→B→A）——未做
P0  fresh release build + 产物 read-back（exe 12:55 构建但无启动验证记录）
P0  BE-B1-001 Session mutation 身份重验——未开始
P0  FE-F4-001 Agent status v2 消费 + FE-F0-009 Agent Sheet 真实切换事务——未开始
P1  安装包 / 干净环境验证
P1  Prism 只读接入（FE-F2-001）
```

## 七、对评估文档的建议更新

1. §4.1/4.2/4.3 三个硬阻塞删除或标记已解决（编译✅ 回归✅ exe✅）
2. §2.1 git 基线更新为 868b0be / ahead 15
3. §2.2/§5.3 标注 91c9864 已集成（9eb1632 merge）
4. §3.3 agents.yaml peri args 补 `--model deepseek-v4-flash`
5. §6 任务卡完成度 3/14
6. 完成度数字上调：内部 Alpha 65% → ~80%；Beta 45% → ~55%
7. "当前不能上线"结论不变，但第一优先级从"恢复编译"改为"真实主链验收"
