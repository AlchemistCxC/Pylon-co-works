REVIEW-v12
═════════

3 commits, 10 files。工作区干净。

✅ §1-3 标题栏重构 — ☰+Tab 左移 + 删 chat-header + Agent 标签动态
✅ §4 中控拖拽 — @dnd-kit 排序 + 显隐 + ccHeight/ccBg
✅ §5 BUGFIX — sessionRef 存 source 替代 id，filter 正确
🟡 §6 字体 — Sarasa Mono SC 改为纯 CSS：
   index.css --mono 删 Sarasa，留 'Cascadia Code', 'Consolas', monospace
   ChatView.css .term 加 font-weight:350; letter-spacing:-0.2px;
   font-feature-settings:"calt" off; -webkit-font-smoothing:auto
